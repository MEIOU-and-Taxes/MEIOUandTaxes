########################################
#                                      #
#     FormingHistoricalNations.txt     #
#                                      #
########################################
#
# 2011-jan-16 FB add ai_importance = 200
# 2011-aug-01 FB fixes to denmark_norway_nation
# 2012-jul-04 GG new requirement : either 80% of provinces or 66% and capital province
# 2013-aug-26 GG EUIV changes
#
########################################
#
# List of decisions :
#
# prussian_nation				=> moved to missions, deleted
# form_kingdom_of_prussia	=> moved to missions, deleted
# anglofrench_nation
# denmark_norway_nation
# spanish_nation
# english_nation
# french_nation
# form_greece_kingdom
# irish_nation
# mughal_nation
# aztec_empire
# mayan_empire
# incan_empire
# netherlands_nation
# unite_turkish_nation
# unite_turkish_republic
# persian_nation
# roumanian_nation
# russian_nation
# saxon_nation
# nation_bretonne
# british_nation
# bulgaria_nation
# italian_nation_no_hre
# tuscany_nation
# sicilian_nation
# german_nation
# bavarian_nation
# swabian_nation
# hannover_nation
# form_sardinia_piedmont
# form_julich_berg_kleve
# form_siam
# form_goldenhorde
# form_celestial_empire
# westphalia_nation
# malaccan_nation
# polish_nation
# polish_lithuanian_nation
# austrian_nation
# egyptian_nation
# become_sultan_of_delhi
# georgian_nation
# assam_nation
#
# To be added later on :
#
# austro_hungarian_nation
#
########################################

########################################
#Temporarily added everything back for 3.0
#Missions that were commented out were already moved to missions, and are marked by comments
#Every formable tag to be moved to missions eventually
#Checked manually for things which are not in 3.0. There was a lot, all commented out now

country_decisions = {
	form_greece_kingdom = {
		major = yes
		potential = {
			OR = {
				culture_group = greek_group
				tag = ATH
				tag = ACH
			}
			NOT = { exists = GRE }
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = BYZ }
			NOT = { tag = OTT }
			NOT = { tag = TUR }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			region = greece_region
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			owns = 146 # Attica
			is_core = 146 # Attica
			is_at_war = no
			OR = {
				check_variable = { which = "Demesne_in_Greece" value = 20 }
				check_variable = { which = "Cores_on_Greece" value = 16 }
			}
		}
		effect = {
			greece_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = GRE } add_core = GRE }
			greece_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GRE }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):form_greece_kingdom"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [GRE.GetName] via decision:form_greece_kingdom"
			ChangeTagEffect = { scope=GRE }
			change_primary_culture = greek
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = GRE_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
			every_country = {
				limit = {
					isValidCountry = yes
					OR = {
						primary_culture = greek
						capital_scope = { region = greece_region }
					}
					NOT = { tag = ROOT }
				}
				add_historical_rival = GRE
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	mughal_nation = {
		major = yes
		potential = {
			religion_group = muslim
			OR = {
				culture_group = altaic
				culture_group = tartar_group
				culture_group = persian_group
			}
			NOT = { exists = MUG }
			NOT = { tag = PER }
			NOT = { tag = TUR }
			OR = {
				owns_or_non_sovereign_subject_of = 521	# Panipat
				owns_or_non_sovereign_subject_of = 522	# Jindh
				owns_or_non_sovereign_subject_of = 524	# Braj
				owns_or_non_sovereign_subject_of = 2163	# Delhi
				owns_or_non_sovereign_subject_of = 2555	# Katehar
				owns_or_non_sovereign_subject_of = 2705	# Mewat
			}
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			continent = indian_continent
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			is_at_war = no
			owns_or_non_sovereign_subject_of = 521		# Panipat
			owns_or_non_sovereign_subject_of = 522		# Jindh
			owns_or_non_sovereign_subject_of = 524		# Braj
			owns_or_non_sovereign_subject_of = 2163		# Delhi
			owns_or_non_sovereign_subject_of = 2555		# Katehar
			owns_or_non_sovereign_subject_of = 2705		# Mewat
			OR = {
				check_variable = { which = "Cores_on_India" value = 20 }
				check_variable = { which = "Demesne_in_India" value = 30 }
			}
		}
		effect = {
			west_bengal_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MUG } add_core = MUG }
			west_bengal_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			east_bengal_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MUG } add_core = MUG }
			east_bengal_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			upper_doab_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MUG } add_core = MUG }
			upper_doab_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			central_doab_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MUG } add_core = MUG }
			central_doab_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			punjab_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MUG } add_core = MUG }
			punjab_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			awadh_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MUG } add_core = MUG }
			awadh_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			bihar_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MUG } add_core = MUG }
			bihar_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			hindusthan_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MUG } add_core = MUG }
			hindusthan_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			west_india_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MUG } add_core = MUG }
			west_india_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MUG }
			add_prestige = 20
			if = {
				limit = {
					government = monarchy
					NOT = { has_country_modifier = title_6 }
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 6 (Empire equivalent):mughal_nation"
				change_title_6 = yes
			}
			if = {
				limit = {
					has_reform = Horde_MonarchyS
					#is_key_equal = { lhs = Pol_Privileges value = 8 } 
				}
				add_government_reform = Pol_Privileges1
			}
			if = {
				limit = {
					technology_group = steppestech
				}
				change_technology_group = muslim
				change_unit_type = indian
			}
			change_primary_culture = urdu
			#set_country_flag = urdu_state
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [MUG.GetName] via decision:mughal_nation"
			ChangeTagEffect = { scope=MUG }
			#despotisation_effect = yes
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = MUG_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
			every_country = {
				limit = {
					isValidCountry = yes
					OR = {
						capital_scope = { region = east_bengal_region }
						capital_scope = { region = west_bengal_region }
						capital_scope = { region = punjab_region }
						capital_scope = { region = central_doab_region }
						capital_scope = { region = upper_doab_region }
						capital_scope = { region = awadh_region }
						capital_scope = { region = west_india_region }
					}
					NOT = { tag = ROOT }
				}
				add_historical_rival = MUG
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	aztec_empire = {
		major = yes
		potential = {
			OR = {
				tag = TEN
				tag = ACO
			}
			NOT = { exists = AZT }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				province_id = 1471	#Tenochtitlan
				province_id = 2200	#Acolhua
				province_id = 852 	#Azcapotzalco
			}
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {			
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
            852 = {
                OR = {
                    owned_by = TEN
                    owned_by = ACO
                }
            }
			NOT = { exists = TEP }
            OR = {
                AND = {
                    tag = TEN
                    OR = {
                        overlord_of = ACO
                        alliance_with = ACO
                    }
                    total_development = ACO
                }
                AND = {
                    tag = ACO
                    OR = {
                        overlord_of = TEN
                        alliance_with = TEN
                    }
                    total_development = TEN
                }
            }
        }
		effect = {
			if = {
                limit = { tag = TEN }
                inherit = ACO
            }
            else = {
                inherit = TEN
            }
			mexico_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = AZT } add_core = AZT }
			chontal_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = AZT } add_core = AZT }
			mexico_region = { limit = { NOT = { owned_by = ROOT } is_city = yes } add_permanent_claim = AZT }
			zacatecas_region = { limit = { NOT = { owned_by = ROOT } is_city = yes } add_permanent_claim = AZT }
			zapotec_region = { limit = { NOT = { owned_by = ROOT } is_city = yes } add_permanent_claim = AZT }
			add_prestige = 50
			if = {
				limit = { NOT = { has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 6 (Empire equivalent):aztec_empire"
				change_title_6 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [AZT.GetName] via decision:aztec_empire"
			ChangeTagEffect = { scope=AZT }
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = AZT_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
			every_country = {
				limit = {
					isValidCountry = yes
					capital_scope = { region = mexico_region }
					NOT = { tag = ROOT }
				}
				add_historical_rival = AZT
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	#mayan_empire = {
	#	major = yes
	#	potential = {
	#		culture_group = maya
	#		NOT = { exists = MAY }
	#		is_colonial_nation = no
	#	}
	#	provinces_to_highlight = {
	#		region = central_america_region
	#	}
	#	allow = {
	#		OR = {
	#			check_variable = { which = "Demesne_in_Maya" value = 15 }
	#			check_variable = { which = "Cores_on_Maya" value = 10 }
	#		}
	#		custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
	#		owns_core_province = 840		# Copan
	#		owns_core_province = 845		# Uxmal
	#		owns_core_province = 846		# Chichen-Itza
	#		owns_core_province = 2208		# Tikal
	#		is_at_war = no
	#	}
	#	effect = {
	#		guatemala_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MAY } add_core = MAY }
	#		guatemala_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MAY }
	#		yucatan_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MAY } add_core = MAY }
	#		yucatan_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MAY }
	#		chiapas_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MAY } add_core = MAY }
	#		chiapas_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MAY }
	#		belize_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MAY } add_core = MAY }
	#		belize_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MAY }
	#		campeche_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MAY } add_core = MAY }
	#		campeche_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MAY }
	#		add_prestige = 20
	#		if = {
	#			limit = {
	#				trib_fed_trigger = yes
	#			}
	#			trib_king_effect = yes
	#		}
	#		if = {
	#			limit = { NOT = { has_country_modifier = title_6 } }
	#			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 6 (Empire equivalent):mayan_empire"
	#			change_title_6 = yes
	#		}
	#		log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [MAY.GetName] via decision:mayan_empire"
	#		ChangeTagEffect = { scope=MAY }
	#		add_absolutism = 10
	#		#if = {
	#		#	limit = {
	#		#		has_custom_ideas = no
	#		#		NOT = { has_idea_group = MAY_ideas }
	#		#	}
	#		#	swap_national_ideas_effect = yes
	#		#}
	#	}
	#	ai_will_do = {
	#		factor = 1
	#	}
	#	ai_importance = 400
	#}
	
	incan_empire = {
		major = yes
		potential = {
			primary_culture = inca
			NOT = { exists = INC }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			region = peru_region
			region = bolivia_region
		}
		allow = {
			OR = {
				check_variable = { which = "Demesne_in_Inca" value = 35 }
				check_variable = { which = "Cores_on_Inca" value = 25 }
			}
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			owns_core_province = 2068			# Cuzco
			OR = {
				owns_core_province = 816		# Pachacamac
				AND = {
					816 = { owned_by = CHC }
					alliance_with = CHC
				}
			}
			OR = {
				owns_core_province = 820		# Huancayo
				owns_core_province = 3435		# lupaca
			}
			OR = {
				owns_core_province = 811		# Kasha Marka
				owns_core_province = 3436		# Golla
			}
			is_at_war = no
		}
		effect = {
			peru_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = INC } add_core = INC }
			peru_region = { limit = { NOT = { owned_by = ROOT } is_city = yes } add_permanent_claim = INC }
			bolivia_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = INC } add_core = INC }
			bolivia_region = { limit = { NOT = { owned_by = ROOT } is_city = yes } add_permanent_claim = INC }
			add_prestige = 20
			INC = { Effect_set_capital = { target=2068 } }
			#if = {
			#	limit = {
			#		OR = {
			#			trib_king_trigger = yes
			#			trib_desp_trigger = yes
			#			trib_fed_trigger = yes
			#		}
			#	}
			#	despotisation_effect = yes
			#}
			if = {
				limit = { NOT = { has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 6 (Empire equivalent):incan_empire"
				change_title_6 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [INC.GetName] via decision:incan_empire"			
			ChangeTagEffect = { scope=INC }
			add_absolutism = 10
			if = {
				limit = {
					AND = {
						816 = { owned_by = CHC }
						alliance_with = CHC
					}
				}
				inherit = CHC
			}
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = INC_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}

	persian_nation = {
		major = yes
		potential = {
			OR = {
				culture_group = persian_group
				tag = JAI
				}
			NOT = { exists = PER }
			NOT = { tag = MUG }
			Timur_Has = no
			OR = {
				owns = 390 # Yazd
				owns = 414 # Isfahan
				#owns = 426 # Mazandaran
				owns = 1313 # Rey
				owns = 1314 # Qasvin
				owns = 2225 # Qom
			}
			OR = {
				ai = no
				NOT = { tag = TIM }
			}
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				region = iran_region
				region = fars_region
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			OR = {
				check_variable = { which = "Demesne_in_Persia" value = 30 }
				check_variable = { which = "Cores_on_Persia" value = 20 }
			}
			owns = 390 # Yazd
			owns = 414 # Isfahan
			owns = 426 # Mazandaran
			owns = 1313 # Rey
			owns = 1314 # Qasvin
			owns = 2225 # Qom
			is_at_war = no
		}
		effect = {
			iran_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PER } add_core = PER }
			iran_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PER }
			fars_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PER } add_core = PER }
			fars_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PER }
			khorasani_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PER } add_core = PER }
			khorasani_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PER }
			sistan_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PER } add_core = PER }
			sistan_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PER }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 6 (Empire equivalent):persian_nation"
				change_title_6 = yes
			}
			if = {
				limit = { 
					NOT = {
						culture_group = persian_group
					}
				}
				change_primary_culture = persian
			}
			if = {
				limit = {
					NOT = { has_idea_group = persian_ideas }
				}
				swap_national_ideas_effect = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [PER.GetName] via decision:persian_nation"
			ChangeTagEffect = { scope=PER }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = PER_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	roumanian_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			culture_group = romanian
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			NOT = { tag = BYZ }
			NOT = { exists = ROU }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				region = south_carpathia_region
				area = transylvania_area
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			OR = {
				check_variable = { which = "Demesne_in_Roumania" value = 16 } #Was 12 - Sirbab
				check_variable = { which = "Cores_on_Roumania" value = 14 }#Was 10 - Sirbab
			}
			is_at_war = no
			adm_tech = 17
			stability = 1
			legitimacy_equivalent = 80
			OR = {
				adm = 5
				dip = 5
				statesman_BG = 2
				statesman_BU = 2
				statesman_CL = 2
				statesman_NO = 2
				statesman_TR = 2
			}
			# OR = { #Commented out to preserve for posperity, and allow for temporary shift in requirements - Sirbab
				# adm_tech = 45
				# #AND = {
				# ADM = 5
					# #statesman = 2
				# #}
				# AND = {
					# NOT = { absolutism = 80 }
					# full_idea_group = administrative_ideas
					# full_idea_group = diplomacy_ideas
				# }
			# }
		}
		effect = {
			south_carpathia_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = ROU } add_core = ROU }
			south_carpathia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ROU }
			add_prestige = 15
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):roumanian_nation"
				change_title_5 = yes
			}
			if = {
				limit = {
					OR = {
						WAL = { is_vassal_of_trigger = { scp_root = yes } }
						WAL = { junior_union_with = ROOT }
				}
			}
				inherit = WAL
			}
			if = {
				limit = {
					OR = {
						MOL = { is_vassal_of_trigger = { scp_root = yes } }
						MOL = { junior_union_with = ROOT }
					}
				}
				inherit = MOL
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [ROU.GetName] via decision:roumanian_nation"
			ChangeTagEffect = { scope=ROU }
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = RMN_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
			add_absolutism = 10
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	russian_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			#always = no
			culture_group = east_slavic
			NOT = { exists = RUS }
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = ROM }
			OR = {
				primary_culture = russian
				primary_culture = novgorodian
				primary_culture = ruthenian
			}
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				region = north_russia_region
				region = russia_region
				region = east_russia_region
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			is_at_war = no
			OR = {
				adm_tech = 25
				ADM = 5
			}
			OR = {
				check_variable = { which = "Demesne_in_Russia" value = 50 }
				check_variable = { which = "Cores_on_Russia" value = 35 }
			}
		}
		effect = {
			north_russia_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = RUS } add_core = RUS }
			north_russia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			russia_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = RUS } add_core = RUS }
			russia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			east_russia_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = RUS } add_core = RUS }
			east_russia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			volga_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			steppes_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			sloboda_ukraine_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = RUS }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 6 (Empire equivalent):russian_nation"
				change_title_6 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [RUS.GetName] via decision:russian_nation"
			ChangeTagEffect = { scope=RUS }
			set_country_flag = rus_formed
			add_absolutism = 10
			
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = RUS_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
			every_country = {
				limit = {
					isValidCountry = yes
					OR = {
						primary_culture = russian
						capital_scope = { region = north_russia_region }
						capital_scope = { region = russia_region }
						capital_scope = { region = east_russia_region }
						tag = LIT
						tag = POL
						tag = PLC
					}
					NOT = { tag = ROOT }
					NOT = { is_subject_of = ROOT }
				}
				add_historical_rival = RUS
				ROOT = { add_historical_rival = PREV }
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
		
	saxon_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			culture_group = high_germanic
			OR = {
				primary_culture = high_saxon
				primary_culture = thuringian
			}
			NOT = { exists = SAX }
			#forming_SAX_trigger = yes
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = WES }
			NOT = { tag = HSA }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			check_variable = { which = "Demesne_in_the_Upper_Saxon_Circuit" value = 5 }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			region = upper_saxon_circle_region
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			is_at_war = no
			check_variable = { which = "Cores_on_the_Upper_Saxon_Circuit" value = 10 }
			OR = {
				owns_core_province = 59		# Meissen
				owns_core_province = 1373 	# Leipzig
				owns_core_province = 2617	# Dresden
			}
		}
		effect = {
			upper_saxon_circle_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SAX } add_core = SAX }
			upper_saxon_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SAX }
			add_prestige = 20
			if = {
				limit = { government = monarchy OR = { has_country_modifier = title_1 has_country_modifier = title_2 } }
				change_title_3 = yes
			}
			if = {
				limit = { NOT = { hre_size = 1 } government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):saxon_nation"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [SAX.GetName] via decision:saxon_nation"
			ChangeTagEffect = { scope=SAX }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = elbe_river_german_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
			#if = {
			#	limit = {
			#		has_country_flag = wettin_succession
			#	}
			#	clr_country_flag = wettin_succession
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	nation_bretonne = { #Exists as automated event in 2.52 IIRC
		major = yes
		potential = {
			OR = {
				tag = MNF
				tag = BLO
			}
			NOT = { exists = BRI }
			#forming_BRI_trigger = yes
			check_variable = { which = "Demesne_in_Brittany" value = 3 }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			area = brittany_area
		}
		allow = {
			OR = {
				AND = {
					tag = MNF
					NOT = { exists = BLO }
				}
				AND = {
					tag = BLO
					NOT = { exists = MNF }
				}
			}
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			is_at_war = no
			check_variable = { which = "Cores_on_Brittany" value = 4 }
		}
		effect = {
			brittany_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = BRI } add_core = BRI }
			brittany_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = BRI }
			add_prestige = 20
			if = {
				limit = { government = monarchy OR = { has_country_modifier = title_1 has_country_modifier = title_2 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 3:nation_bretonne"
				change_title_3 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [BRI.GetName] via decision:nation_bretonne"
			ChangeTagEffect = { scope=BRI }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = breton_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	bulgaria_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			primary_culture = bulgarian
			NOT = { exists = BUL }
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = BYZ }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				province_id = 148	# Ohrid
				province_id = 150	# Silistria
				province_id = 156	# Nisava
				province_id = 157	# Lodogoria
				province_id = 159	# Dobrogea
				province_id = 1426	# Serdica
				province_id = 1427	# Maritsa
				province_id = 2381	# Karvuna
				province_id = 2382	# Vidin
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			is_at_war = no
			owns = 148	# Ohrid
			owns = 150	# Silistria
			owns = 156	# Nisava
			owns = 157	# Lodogoria
			owns = 159	# Dobrogea
			owns = 1426	# Serdica
			owns = 1427	# Maritsa
			owns = 2381	# Karvuna
			owns = 2382	# Vidin
		}
		effect = {
			north_bulgaria_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = BUL } add_core = BUL }
			north_bulgaria_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = BUL }
			south_bulgaria_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = BUL } add_core = BUL }
			south_bulgaria_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = BUL }
			dobrogea_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = BUL } add_core = BUL }
			dobrogea_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = BUL }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):bulgaria_nation"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [BUL.GetName] via decision:bulgaria_nation"
			ChangeTagEffect = { scope=BUL }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = BUL_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	serbia_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			#always = no
			NOT = { exists = SER }
			primary_culture = serbian
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = BYZ }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			NOT = { tag = ILL }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			serbia_region_trigger = yes
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			is_at_war = no
			# owns = 2612	# Belgrade
			OR = {
				check_variable = { which = "Demesne_in_Serbia" value = 8 }
				check_variable = { which = "Cores_on_Serbia" value = 6 }
			}
		}
		effect = {
			south_serbia_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SER } add_core = SER }
			south_serbia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SER }
			north_serbia_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SER } add_core = SER }
			north_serbia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SER }
			west_serbia_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SER } add_core = SER }
			west_serbia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SER }
			macedonia_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SER } add_core = SER }
			macedonia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SER }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):serbia_nation"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [SER.GetName] via decision:serbia_nation"
			ChangeTagEffect = { scope=SER }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = SER_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	sicilian_nation = {  #Two Sicilies
		major = yes
		potential = {
			#always = no
			OR = {
				primary_culture = neapolitan
				primary_culture = sicilian
			}
			NOT = { exists = NAP }
			OR = { #There can only be one Sicily alive
				AND = {
					tag = SIC
					NOT = { exists = KNP }
				}
				AND = {
					tag = KNP
					NOT = { exists = SIC }
				}
			}
			NOT = { is_emperor = yes }
			is_colonial_nation = no
			#adm_tech = 30             	#Only makes the decision not show up, making it impossible to see the requirements for the player early
			capital_scope = { 
				OR = {
					region = sicily_region
					region = naples_region
				}
		}
		provinces_to_highlight = {
			OR = {
				region = sicily_region
				region = naples_region
			}
		}
		allow = {
			government = monarchy
			prestige = 25 #reduced from 50 (lower resting point in 3.0)
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			OR = {
				adm_tech = 40
				AND = {
					NOT = { absolutism = 90 }
					stability = 3
					full_idea_group = administrative_ideas
					full_idea_group = culture_ideas
					full_idea_group = diplomacy_ideas
				}
				AND = {
					ADM = 5
					OR = {
						advisor = statesman_CL
						advisor = statesman_BG
						advisor = statesman_NO
						advisor = statesman_BU
						advisor = statesman_TR
					}
				}
			}
			owns = 2531	# Napoli
			owns = 125	# Palermu
			OR = {
				check_variable = { which = "Demesne_in_Two_Sicilies" value = 20 }
				check_variable = { which = "Cores_on_Two_Sicilies" value = 15 }
			}
			is_at_war = no
		}
		effect = {
			sicily_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = NAP } add_core = NAP }
			sicily_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = NAP }
			naples_region = { limit = { owned_by = ROOT } add_core = NAP }
			naples_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = NAP }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):sicilian_nation"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [NAP.GetName] via decision:sicilian_nation"
			ChangeTagEffect = { scope=NAP }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = neapolitan_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	german_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			OR = {
				culture_group = high_germanic
				culture_group = low_germanic
			}
			NOT = { exists = GER }
			OR = {
				NOT = { tag = ERG }
				AND = {
					dip_tech = 55
					NOT = {
						tag = ERG
						ai = yes
					}
				}
			}
			NOT = { tag = PAP }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				superregion = germany_superregion
				region = austrian_circle_region
				region = swabia_bavarian_region
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			OR = {
				check_variable = { which = "Demesne_in_Germany" value = 70 }
				AND = {
					check_variable = { which = "Cores_on_Bavarian_Circuit" value = 3 }
					check_variable = { which = "Cores_on_Franconian_Circuit" value = 3 }
					check_variable = { which = "Cores_on_the_Swabian_Circuit" value = 6 }
					check_variable = { which = "Cores_on_the_Upper_Saxon_Circuit" value = 10 }
					check_variable = { which = "Cores_on_the_Lower_Saxon_Circuit" value = 10 }
					check_variable = { which = "Cores_on_the_Upper_Rhine_Circuit" value = 9 }
					check_variable = { which = "Cores_on_Westphalia" value = 4 }
				}
			}
			is_at_war = no
			OR = {
				NOT = { hre_size = 1 }
				AND = {
					hre_size = 1
					NOT = { hre_size = 2 }
					is_emperor = yes
				}
				adm_tech = 60
			}
		}
		effect = {
			lower_saxon_circle_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = GER } add_core = GER }
			lower_saxon_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GER }
			upper_saxon_circle_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = GER } add_core = GER }
			upper_saxon_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GER }
			westphalian_circle_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = GER } add_core = GER }
			westphalian_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GER }
			upper_rhenish_circle_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = GER } add_core = GER }
			upper_rhenish_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GER }
			swabia_bavarian_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = GER } add_core = GER }
			swabia_bavarian_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GER }
			#all_core_province = { limit = { owned_by = ROOT } } add_core = GER }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):german_nation"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [GER.GetName] via decision:german_nation"
			ChangeTagEffect = { scope=GER }
			add_absolutism = 10
			emperor = {
				add_opinion = {
					who = root
					modifier = opinion_left_empire
				}
			}
			leave_the_hre = yes
			#emperor = {
			#	country_event = { id = hre_event.5 }
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	bavarian_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			always = no
			primary_culture = bavarian
			NOT = { exists = BAV }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = WES }
			NOT = { tag = HSA }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				area = lower_bavaria_area
				area = upper_bavaria_area
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			check_variable = { which = "Demesne_in_Bavarian_Circuit" value = 8 }
			is_core = 64
			is_core = 65
			is_core = 1351
			is_core = 1509
			is_at_war = no
		}
		effect = {
			lower_bavaria_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = BAV } add_core = BAV }
			lower_bavaria_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = BAV }
			upper_bavaria_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = BAV } add_core = BAV }
			upper_bavaria_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = BAV }
			add_prestige = 20
			if = {
				limit = { government = monarchy OR = { has_country_modifier = title_1 has_country_modifier = title_2 } }
				change_title_3 = yes
			}
			if = {
				limit = { NOT = { hre_size = 1 } government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):bavarian_nation"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [BAV.GetName] via decision:bavarian_nation"
			ChangeTagEffect = { scope=BAV }
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = danubian_alpine_german_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
			add_absolutism = 10
			#swap_non_generic_missions = yes
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	swabian_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			primary_culture = schwabisch
			NOT = { exists = SWA }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = WES }
			NOT = { tag = HSA }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				area = lower_swabia_area
				area = upper_swabia_area
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			check_variable = { which = "Demesne_in_the_Swabian_Circuit" value = 8 }
			is_core = 77
			
			is_at_war = no
		}
		effect = {
			lower_swabia_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SWA } add_core = SWA }
			lower_swabia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SWA }
			upper_swabia_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SWA } add_core = SWA }
			upper_swabia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SWA }
			add_prestige = 20
			if = {
				limit = { government = monarchy OR = { has_country_modifier = title_1 has_country_modifier = title_2 } }
				change_title_3 = yes
			}
			if = {
				limit = { NOT = { hre_size = 1 } government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):swabian_nation"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [SWA.GetName] via decision:swabian_nation"
			ChangeTagEffect = { scope=SWA }
			add_absolutism = 10
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	lower_saxony_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			culture_group = low_germanic
			OR = {
				primary_culture = old_saxon
				primary_culture = eastphalian
				primary_culture = westphalian
			}
			NOT = { exists = LSA }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = WES }
			NOT = { tag = HSA }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				area = eastphalia_area
				area = holstein_area
				area = weser_area
				area = ems_area
				area = westphalia_area
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			is_at_war = no
			OR = {
				check_variable = { which = "Demesne_in_the_Lower_Saxon_Circuit" value = 16 }
				check_variable = { which = "Cores_on_the_Lower_Saxon_Circuit" value = 11 }
			}
		}
		effect = {
			eastphalia_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = LSA } add_core = LSA }
			eastphalia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = LSA }
			holstein_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = LSA } add_core = LSA }
			holstein_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = LSA }
			weser_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = LSA } add_core = LSA }
			weser_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = LSA }
			ems_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = LSA } add_core = LSA }
			ems_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = LSA }
			westphalia_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = LSA } add_core = LSA }
			westphalia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = LSA }
			add_prestige = 20
			if = {
				limit = { government = monarchy OR = { has_country_modifier = title_1 has_country_modifier = title_2 } }
				change_title_3 = yes
			}
			if = {
				limit = { NOT = { hre_size = 1 } government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):lower_saxony_nation"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [LSA.GetName] via decision:lower_saxony_nation"
			ChangeTagEffect = { scope=LSA }
			add_absolutism = 10
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	form_sardinia_piedmont = {
		major = yes
		potential = {
			culture_group = latin
			NOT = { exists = SPI }
			NOT = { exists = ITA }
			NOT = { tag = ERG }
			NOT = { tag = PAP }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			capital_scope = { OR = { area = piedmont_area area = liguria_area area = corsica_sardinia_area } }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				area = piedmont_area
				area = liguria_area
				area = corsica_sardinia_area
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			OR = {
				adm_tech = 45
				AND = {
					NOT = { absolutism = 80 }
					stability = 1
					full_idea_group = diplomacy_ideas
				}
				AND = {
					adm_tech = 29
					ADM = 5
					OR = {
						advisor = statesman_CL
						advisor = statesman_BG
						advisor = statesman_NO
						advisor = statesman_BU
						advisor = statesman_TR
					}
				}
			}
			owns = 103 # Piemonte
			OR = {
				AND = {
					check_variable = { which = "Demesne_in_Liguria_Piedmont" value = 5 }
					check_variable = { which = "Demesne_in_Sardinia" value = 4 }
				}
				AND = {
					check_variable = { which = "Cores_on_Liguria_Piedmont" value = 4 }
					check_variable = { which = "Cores_on_Sardinia" value = 3 }
				}
			}
			is_at_war = no
		}
		effect = {
			corsica_sardinia_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SPI } add_core = SPI }
			corsica_sardinia_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SPI }
			piedmont_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SPI } add_core = SPI }
			piedmont_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SPI }
			liguria_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SPI } add_core = SPI }
			liguria_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SPI }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):form_sardinia_piedmont"
				change_title_5 = yes
			}
			if = {
				limit = {
					OR = {
						SAR = { is_vassal_of_trigger = { scp_root = yes } }
						SAR = { junior_union_with = ROOT }
					}
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:inherited [SPI.GetName] while forming [LSA.GetName] via decision:form_sardinia_piedmont"
				inherit = SAR
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [SPI.GetName] via decision:form_sardinia_piedmont"
			ChangeTagEffect = { scope=SPI }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = SPI_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	form_siam = {
		major = yes
		potential = {
			culture_group = southeastasian_group
			is_year = 1700
			NOT = { tag = SUV }
			NOT = { exists = SIA }
			#num_of_cities = 16
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				region = lan_xang_region
				region = inland_siam_region
				region = coastal_siam_region
			}
		}
		allow = {
			OR = {
				AND = {
					check_variable = { which = "Demesne_in_Thai_Plain" value = 10 }
					check_variable = { which = "Demesne_in_Lan_Xang" value = 9 }
				}
				AND = {
					check_variable = { which = "Cores_on_Thai_Plain" value = 8 }
					check_variable = { which = "Cores_on_Lan_Xang" value = 7 }
				}
			}
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			owns = 600  #Ayutthaya
			owns = 601  #Sukhotai
			owns = 602  #Lopburi
			is_at_war = no
		}
		effect = {
			lan_xang_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SIA } add_core = SIA }
			lan_xang_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SIA }
			inland_siam_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SIA } add_core = SIA }
			inland_siam_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SIA }
			coastal_siam_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = SIA } add_core = SIA }
			coastal_siam_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = SIA }
			add_prestige = 20
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [SIA.GetName] via decision:form_siam"
			ChangeTagEffect = { scope=SIA }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = AYU_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	form_goldenhorde = {
		major = yes
		potential = {
			OR = {
				tag = BLU
				tag = CHU
				tag = GHO
				AND = {
					tag = WHI
					is_year = 1365 # Allows the White Horde collapse to occur first
				}
			}
			NOT = { exists = GOL }
		}
		provinces_to_highlight = {
			OR = {
				region = steppes_region
				region = kazakh_region
				region = crimea_region
				region = volga_region
			}
		}
		allow = {
			OR = {
				AND = {
					tag = BLU
					OR = {
						NOT = { exists = WHI }
						AND = {
							overlord_of = WHI
							WHI = {
								is_subject_other_than_tributary_trigger = yes
							}
						}
					}
					OR = {
						NOT = { exists = CHU }
						AND = {
							overlord_of = CHU
							CHU = {
								is_subject_other_than_tributary_trigger = yes
							}
						}
					}
				}
				AND = {
					tag = WHI
					OR = {
						NOT = { exists = BLU }
						AND = {
							overlord_of = BLU
							BLU = {
								is_subject_other_than_tributary_trigger = yes
							}
						}
					}
					OR = {
						NOT = { exists = CHU }
						AND = {
							overlord_of = CHU
							CHU = {
								is_subject_other_than_tributary_trigger = yes
							}
						}
					}
				}
				AND = {
					tag = CHU
					OR = {
						NOT = { exists = BLU }
						AND = {
							overlord_of = BLU
							BLU = {
								is_subject_other_than_tributary_trigger = yes
							}
						}
					}
					OR = {
						NOT = { exists = WHI }
						AND = {
							overlord_of = WHI
							WHI = {
								is_subject_other_than_tributary_trigger = yes
							}
						}
					}
				}
			}
			OR = {
				check_variable = { which = "Demesne_in_Altaic_Throne" value = 28 }
				check_variable = { which = "Cores_on_Altaic_Throne" value = 21 }
			}
			1293 = { country_or_vassal_holds = ROOT } # Astrakhan
			1310 = { country_or_vassal_holds = ROOT } # Sarai-al-Jadid
			2444 = { country_or_vassal_holds = ROOT } # Sarai Juk
			has_heir = yes
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			has_any_disaster = no
			is_at_war = no
		}
		effect = {
			steppes_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = GOL } add_core = GOL }
			steppes_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GOL }
			kazakh_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = GOL } add_core = GOL }
			kazakh_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GOL }
			crimea_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = GOL } add_core = GOL }
			crimea_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GOL }
			volga_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = GOL } add_core = GOL }
			volga_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = GOL }
			if = {
				limit = {
					BLU = { is_subject_other_than_tributary_trigger = yes ai = yes }
				}
				inherit = BLU
			}
			if = {
				limit = {
					CHU = { is_subject_other_than_tributary_trigger = yes ai = yes }
				}
				inherit = CHU
			}
			if = {
				limit = {
					WHI = { is_subject_other_than_tributary_trigger = yes ai = yes }
				}
				inherit = WHI
			}
			add_prestige = 20
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [GOL.GetName] via decision:form_goldenhorde"
			ChangeTagEffect = { scope=GOL }
			add_absolutism = 10
			Effect_set_capital = { target=1310 }
			hidden_effect = {
				if = {
					limit = {
						NOT = { exists = WHI }
					}
					every_province = {
						limit = {
							isValidProv = yes
						}
						remove_core = WHI
					}
				}
				if = {
					limit = {
						NOT = { exists = BLU }
					}
					every_province = {
						limit = {
							isValidProv = yes
						}
						remove_core = BLU
					}
				}
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	westphalia_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			OR = {
				primary_culture = hessian
				primary_culture = old_saxon
			}
			NOT = { exists = WES }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = USA }
			NOT = { tag = BRZ }
			NOT = { tag = MEX }
			NOT = { tag = LSA }
			NOT = { tag = HSA }
			is_colonial_nation = no
		}
		allow = {
			is_at_war = no
			OR = {
				adm_tech = 60
				NOT = { hre_size = 1 }
			}
			OR = {
				check_variable = { which = "Demesne_in_the_Lower_Rhine_Circuit" value = 5 }
				check_variable = { which = "Cores_on_the_Lower_Rhine_Circuit" value = 3 }
			}
		}
		effect = {
			westphalian_circle_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = WES } add_core = WES }
			westphalian_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = WES }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):westphalia_nation"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [WES.GetName] via decision:westphalia_nation"
			ChangeTagEffect = { scope=WES }
			add_absolutism = 10
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	malaccan_nation = {
		major = yes
		potential = {
			primary_culture = malayan
			NOT = { exists = MLC }
			NOT = { tag = MPH }
			NOT = { tag = SRV }
			NOT = { tag = IND }
			check_variable = { which = "Demesne_in_Malaya_Peninsula" value = 1 }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			region = malaya_region
		}
		allow = {
			is_at_war = no
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			OR = {
				check_variable = { which = "Demesne_in_Malaya_Peninsula" value = 12 }
				check_variable = { which = "Cores_on_Malaya_Peninsula" value = 8 }
			}
			owns = 596 # Selangor
		}
		effect = {
			malaya_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MLC } add_core = MLC }
			malaya_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MLC }
			#596 = {
				# add_base_tax = 4
				# add_base_production = 4
				#add_building = fort_15th
			#}
			MLC = { Effect_set_capital = { target=596 } }
			add_prestige = 20
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [MLC.GetName] via decision:malaccan_nation"
			ChangeTagEffect = { scope=MLC }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = MLC_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	polish_nation = {
		major = yes
		potential = {
			OR = {
				tag = MAZ
				AND = {
					primary_culture = silesian
					dynasty = "Piast"
				}
			}
			NOT = { exists = POL }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				region = polonia_major_region
				region = polonia_minor_region
			}
		}
		allow = {
			is_at_war = no
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			OR = {
				check_variable = { which = "Demesne_in_Poland" value = 18 }
				check_variable = { which = "Cores_on_Poland" value = 12 }
			}
			#owns = 262 # Kraków
		}
		effect = {
			polonia_major_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = POL } add_core = POL }
			polonia_major_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = POL }
			polonia_minor_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = POL } add_core = POL }
			polonia_minor_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = POL }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):polish_nation"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [POL.GetName] via decision:polish_nation"
			ChangeTagEffect = { scope=POL }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = POL_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	lithuanian_nation = {
		major = yes
		potential = {
			OR = {
				primary_culture = lithuanian
				AND = {
					primary_culture = ruthenian
					dynasty = "Gediminai"
				}
			}
			NOT = { exists = LIT }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			NOT = { tag = RUS }
			NOT = { tag = PLC }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				region = lithuania_region
				region = ruthenia_region
				region = belarus_region
			}
		}
		allow = {
			is_at_war = no
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			check_variable = { which = "Cores_on_Lithuania" value = 10 }
			owns = 272 # Aukstaitija
		}
		effect = {
			lithuania_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = ROOT } add_core = LIT }
			lithuania_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = LIT }
			ruthenia_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = ROOT } add_core = LIT }
			ruthenia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = LIT }
			belarus_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = ROOT } add_core = LIT }
			belarus_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = LIT }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):lithuanian_nation"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [LIT.GetName] via decision:lithuanian_nation"
			ChangeTagEffect = { scope=LIT }
			add_absolutism = 5
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = LIT_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	polish_lithuanian_nation_dip = {
		major = yes
		potential = {
			tag = POL
			NOT = { exists = PLC }
		}
		allow = {
			is_at_war = no
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			OR = {
				adm_tech = 30
				AND = {
					OR = {
						ADM = 5
						DIP = 5
					}
					adm_tech = 25
				}
			}
			LIT = { is_subject_of = ROOT }
			custom_trigger_tooltip = { tooltip="flavor_pol.union.1" has_country_flag = 1_union_krewo }
			custom_trigger_tooltip = { tooltip="flavor_pol.union.2" has_country_flag = 2_union_horodlo }
			custom_trigger_tooltip = { tooltip="flavor_pol.union.3" has_country_flag = 3_union_grodno }
			custom_trigger_tooltip = { tooltip="flavor_pol.union.4" has_country_flag = 4_union_krakow }
			custom_trigger_tooltip = { tooltip="flavor_pol.union.5" has_country_flag = 5_union_lublin }
		}
		effect = {
			polonia_major_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PLC } add_core = PLC }
			polonia_major_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			polonia_minor_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PLC } add_core = PLC }
			polonia_minor_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			lithuania_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PLC } add_core = PLC }
			lithuania_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			belarus_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PLC } add_core = PLC }
			belarus_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			halych_volhynia_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PLC } add_core = PLC }
			halych_volhynia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			ruthenia_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PLC } add_core = PLC }
			ruthenia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):polish_lithuanian_nation_dip"
				change_title_5 = yes
			}
			if = {
				limit = {
					OR = {
						LIT = { is_vassal_of_trigger = { scp_root = yes } }
						LIT = { junior_union_with = ROOT }
					}
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:inherited [LIT.GetName] while forming [PLC.GetName] via decision:polish_lithuanian_nation_dip"
				inherit = LIT
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [PLC.GetName] via decision:polish_lithuanian_nation_dip"
			ChangeTagEffect = { scope=PLC }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = POL_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	polish_lithuanian_nation = {
		major = yes
		potential = {
			tag = POL
			NOT = { exists = PLC }
		}
		provinces_to_highlight = {
			OR = {
				region = lithuania_region
				region = ruthenia_region
				region = halych_volhynia_region
				region = belarus_region
			}
		}
		allow = {
			is_at_war = no
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			OR = {
				adm_tech = 30
				AND = {
					ADM = 5
					OR = {
						advisor = statesman_CL
						advisor = statesman_BG
						advisor = statesman_NO
						advisor = statesman_BU
						advisor = statesman_TR
					}
					full_idea_group = aristocracy_ideas
				}
			}
			NOT = { exists = LIT }
			OR = {
				check_variable = { which = "Demesne_in_Lithuania" value = 30 }
				check_variable = { which = "Cores_on_Lithuania" value = 20 }
			}
		}
		effect = {
			polonia_major_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PLC } add_core = PLC }
			polonia_major_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			polonia_minor_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PLC } add_core = PLC }
			polonia_minor_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			lithuania_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PLC } add_core = PLC }
			lithuania_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			ruthenia_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PLC } add_core = PLC }
			ruthenia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			halych_volhynia_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PLC } add_core = PLC }
			halych_volhynia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			belarus_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = PLC } add_core = PLC }
			belarus_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = PLC }
			add_prestige = 20
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_5 has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):polish_lithuanian_nation"
				change_title_5 = yes
			}
			if = {
				limit = {
					OR = {
						LIT = { is_vassal_of_trigger = { scp_root = yes } }
						LIT = { junior_union_with = ROOT }
					}
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:inherited [LIT.GetName] while forming [PLC.GetName] via decision:polish_lithuanian_nation"
				inherit = LIT
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [PLC.GetName] via decision:polish_lithuanian_nation"
			ChangeTagEffect = { scope=PLC }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = POL_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	austrian_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			OR = {
				primary_culture = austrian
				primary_culture = slovenian
			}
			NOT = { exists = HAB }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = WES }
			NOT = { tag = HSA }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			government = monarchy
		}
		provinces_to_highlight = {
			region = austrian_circle_region
		}
		allow = {
			is_at_war = no
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			OR = {
				check_variable = { which = "Demesne_in_Austrian_Circuit" value = 5 }
				check_variable = { which = "Cores_on_Austrian_Circuit" value = 3 }
				owns = 83 # Ostmark
			}
		}
		effect = {
			austrian_circle_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = HAB } add_core = HAB }
			austrian_circle_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = HAB }
			add_prestige = 10
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [HAB.GetName] via decision:austrian_nation"
			ChangeTagEffect = { scope=HAB }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = danubian_alpine_german_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	egyptian_nation_foreign = {
		major = yes
		potential = {
			OR = {
				primary_culture = egyptian
				#primary_culture = oriental_culture
			}
			NOT = { exists = MAM }
			NOT = { exists = EGY }
		}
		provinces_to_highlight = {
			region = egypt_region
		}
		allow = {
			is_at_war = no
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			egypt_region = { owned_by = ROOT }
			capital_scope = { region = egypt_region }
		}
		effect = {
			egypt_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = EGY } add_core = EGY }
			egypt_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = EGY }
			#if = {
			#	limit = {
			#		electivisation_trigger = yes
			#	}
			#	administratisation_effect = yes
			#}
			add_prestige = 10
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [EGY.GetName] via decision:egyptian_nation_foreign"
			ChangeTagEffect = { scope=EGY }
			add_absolutism = 10
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	egyptian_nation = {
		major = yes
		potential = {
			tag = MAM
			NOT = { exists = EGY }
		}
		provinces_to_highlight = {
			region = egypt_region
		}
		allow = {
			has_institution = Nationalism
			is_at_war = no
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			egypt_region = { owned_by = ROOT }
		}
		effect = {
			egypt_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = EGY } add_core = EGY }
			egypt_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = EGY }
			if = {
				limit = {
					government = monarchy
					has_reform = Pol_Succession2
					#is_key_equal = { lhs = Pol_Succession value = 1 }
				}
				add_government_reform = Pol_Succession1
			}
			add_prestige = 10
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [EGY.GetName] via decision:egyptian_nation"
			ChangeTagEffect = { scope=EGY }
			add_absolutism = 10
			hidden_effect = {
				every_province = {
					limit = {
						isValidProv = yes
					}
					remove_core = MAM
				}					
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	jurchen_nation = {
		major = yes
		potential = {
			culture_group = jurchen_group
			NOT = { primary_culture = hulun }
			NOT = { exists = MCH }
			NOT = { tag = HUL }
			NOT = { tag = QNG }
		}
		provinces_to_highlight = {
			culture_group = jurchen_group
			owner = { culture_group = jurchen_group }
		}
		allow = {
			OR = {
				custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
				is_subject_of_type = commission
			}
			is_at_war = no
			OR = {
				custom_trigger_tooltip = {
					num_of_owned_provinces_with = {
						value = 18
						culture_group = jurchen_group
					}
					tooltip = jurchen_nation_claim
				}
				custom_trigger_tooltip = {
					tooltip = jurchen_nations
					AND = {
						OR = {
							tag = JTU
							NOT = {
								exists = JTU
							}
							JTU = {
								is_subject_of = ROOT
							}
						}
						OR = {
							tag = JOD
							NOT = {
								exists = JOD
							}
							JOD = {
								is_subject_of = ROOT
							}
						}
						OR = {
							tag = JHO
							NOT = {
								exists = JHO
							}
							JHO = {
								is_subject_of = ROOT
							}
						}
						OR = {
							tag = JHU
							NOT = {
								exists = JHU
							}
							JHU = {
								is_subject_of = ROOT
							}
						}
					}
				}
			}
		}
		effect = {
			add_prestige = 20
			add_manpower = 20
			ChangeTagEffect = { scope=MCH }
			if = {
				limit = {
					overlord_of = JOD
					JOD = { is_subject_other_than_tributary_trigger = yes }
				}
				inherit = JOD
			}
			if = {
				limit = {
					overlord_of = JTU
					JTU = { is_subject_other_than_tributary_trigger = yes }
				}
				inherit = JTU
			}
			if = {
				limit = {
					overlord_of = JHO
					JHO = { is_subject_other_than_tributary_trigger = yes }
				}
				inherit = JHO
			}
			if = {
				limit = {
					overlord_of = JHU
					JHU = { is_subject_other_than_tributary_trigger = yes }
				}
				inherit = JHU
			}
			add_absolutism = 10
			add_adm_power = 500
			add_dip_power = 500
			add_mil_power = 500
			if = {
				limit = {
					monarchy_or_harem_gov = yes
					NOT = {
						has_country_modifier = title_5
						has_country_modifier = title_6
					}
				}
				change_title_5 = yes
			}
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = MCH_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
			east_manchuria_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = ROOT } add_core = MCH }
			east_manchuria_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MCH }
			west_manchuria_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = ROOT } add_core = MCH }
			west_manchuria_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MCH }
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	tibetan_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			culture_group = tibetic
			NOT = { exists = TIB }
			is_free_or_tributary_trigger = yes
		}
		provinces_to_highlight = {
			region = tibet_region
		}
		allow = {
			is_at_war = no
			custom_trigger_tooltip = {
				num_of_owned_provinces_with = {
					value = 15
					region = tibet_region
				}
				tooltip = tibetan_nation_claim
			}
		}
		effect = {
			add_prestige = 20
			tibet_region = { limit = { owned_by = ROOT } add_core = TIB }
			tibet_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = TIB }
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [TIB.GetName] via decision:tibetan_nation"
			ChangeTagEffect = { scope=TIB }
			if = {
				limit = {
					government = monarchy
					NOT = { has_country_modifier = title_6 }
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 6 (Empire equivalent):tibetan_nation"
				change_title_6 = yes
			}
			#f = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = TIB_ideas_dg }
			#	}
			#	swap_national_ideas_effect = yes
			#
			if = {
				limit = {
					BHU = { is_vassal_of_trigger = { scp_root = yes } }
				}
				inherit = BHU
			}
			if = {
				limit = {
					MDO = { is_vassal_of_trigger = { scp_root = yes } }
				}
				inherit = MDO
			}
			if = {
				limit = {
					GUG = { is_vassal_of_trigger = { scp_root = yes } }
				}
				inherit = GUG
			}
			if = {
				limit = {
					UTS = { is_vassal_of_trigger = { scp_root = yes } }
				}
				inherit = UTS
			}
			if = {
				limit = {
					LDK = { is_vassal_of_trigger = { scp_root = yes } }
				}
				inherit = LDK
			}
			if = {
				limit = {
					SKK = { is_vassal_of_trigger = { scp_root = yes } }
				}
				inherit = SKK
			}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	pomeranian_nation = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			primary_culture = pommeranian
			NOT = { exists = POM }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = GER }
			NOT = { tag = PRU }
			NOT = { tag = WES }
			NOT = { tag = HSA }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			capital_scope = { or = { area = vorpommern_area area = hinterpommern_area } }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				province_id = 47
				province_id = 2483
				province_id = 2844
				province_id = 2847
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			num_of_owned_provinces_with = {
				value = 4
				or = { area = vorpommern_area area = hinterpommern_area }
			}
			owns_core_province = 47 # Vorpommern
			owns_core_province = 2843 # Stralsund
			owns_core_province = 2844 # Pyritz
			owns_core_province = 2847 # Stargard
			is_at_war = no
		}
		effect = {
			vorpommern_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = POM } add_core = POM }
			vorpommern_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = POM }
			hinterpommern_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = POM } add_core = POM }
			hinterpommern_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = POM }
			add_prestige = 20
			if = {
				limit = {
					government = monarchy
					OR = {
						has_country_modifier = title_1
						has_country_modifier = title_2
					}
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 3:pomeranian_nation"
				change_title_3 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [POM.GetName] via decision:pomeranian_nation"
			ChangeTagEffect = { scope=POM }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = elbe_river_german_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	become_sultan_of_delhi = {
		major = yes
		potential = {
			owns_or_non_sovereign_subject_of = 2163		# Delhi
			religion_group = muslim
			capital_scope = {
				indian_region_trigger = yes
			}
			#NOT = { has_country_flag = ruler_of_the_deccan_flag }
			#NOT = { has_country_flag = ruler_of_dravidia_flag }
			OR = {
				NOT = { exists = DLH }
				AND = {
					overlord_of = DLH
					DLH = { is_subject_other_than_tributary_trigger = yes }
				}
			}
			NOT = { tag = MUG }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			continent = indian_continent
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			is_at_war = no
			OR = {
				check_variable = { which = "Cores_on_India" value = 5 }
				check_variable = { which = "Demesne_in_India" value = 10 }
			}
		}
		effect = {
			add_prestige = 20
			if = {
				limit = {
					overlord_of = DLH
					DLH = { is_subject_other_than_tributary_trigger = yes }
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:inherited [DLH.GetName] while forming [DLH.GetName] via decision:become_sultan_of_delhi"
				inherit = DLH
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [DLH.GetName] via decision:become_sultan_of_delhi"
			ChangeTagEffect = { scope=DLH }
			Effect_set_capital = { target=2163 }
			change_primary_culture = capital
			#remove_country_modifier = blessing_of_god
			every_owned_province = {
				limit = {
					indian_region_trigger = yes
					NOT = { is_core = DLH }
				}
				hidden_effect = { remove_core = DLH } add_core = DLH
			}
			every_owned_province = {
				limit = {
					indian_region_trigger = yes
					NOT = { is_core = DLH }
				}
				add_territorial_core_effect = yes
			}
			if = {
				limit = {
					government = monarchy
					NOT = { has_country_modifier = title_6 }
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 6 (Empire equivalent):become_sultan_of_delhi"
				change_title_6 = yes
			}
			#if = {
			#	limit = {
			#		trib_king_trigger = yes
			#	}
			#	despotisation_effect = yes
			#}
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = DLH_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	georgian_nation = {
		major = yes
		potential = {
			primary_culture = georgian
			NOT = { exists = GEO }
			NOT = { tag = BYZ }
			NOT = { tag = PAP }
			NOT = { tag = ERG }
			NOT = { tag = ROM }
			NOT = { tag = LAT }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				province_id = 422
				province_id = 423
				province_id = 1301
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			is_at_war = no
			owns_core_province = 422 # Kakheti
			owns_core_province = 423 # Kartli
			owns_core_province = 1301 # Imeretia
		}
		effect = {
			2549 = { add_core = GEO } # Jiqeti
			1300 = { add_core = GEO } # Abkhazia
			1449 = { add_core = GEO } # Klarjeti
			2581 = { add_core = GEO } # Kars
			419 = { add_core = GEO } # Hayastan
			2576 = { add_core = GEO } # Herethi
			add_prestige = 20
			if = {
				limit = {
					government = monarchy
					OR = {
						has_country_modifier = title_1
						has_country_modifier = title_2
						has_country_modifier = title_3
					}
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 4:georgian_nation"
				change_title_4 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [GEO.GetName] via decision:georgian_nation"
			ChangeTagEffect = { scope=GEO }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = GEO_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	assam_nation = {
		major = yes
		potential = {
			culture_group = assamese_group
			NOT = { exists = ASS }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				province_id = 570
				province_id = 571
				province_id = 3193
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			is_at_war = no
			owns_core_province = 3193 # Sadiya
			owns_core_province = 571 # Brahmaputra
			owns_core_province = 570 # Ahom
		}
		effect = {
			3193 = { add_core = ASS } # Sadiya
			571 = { add_core = ASS } # Brahmaputra
			570 = { add_core = ASS } # Ahom
			566 = { add_core = ASS } # Darrang
			2695 = { add_core = ASS } # Koch Hajo
			add_prestige = 20
			if = {
				limit = {
					government = monarchy
					NOT = {
						has_country_modifier = title_5
						has_country_modifier = title_6
					}
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):assam_nation"
				change_title_5 = yes
			}
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [ASS.GetName] via decision:assam_nation"
			ChangeTagEffect = { scope=ASS }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = ASS_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	
	
	form_morocco = { #an alternative to "Fez" for countires that otherwise meet the requirments and want a unification #Already exists in missions, to be removed when the relevant missions get added back in
		potential = {
			religion_group = muslim
			OR = {
				primary_culture = tuareg
				primary_culture = fassi
				primary_culture = berber
				primary_culture = andalucian
				tag = GRA
				tag = SOS
				tag = TFL
			}
			NOT = { tag = FEZ }
			NOT = { tag = TLE }
			NOT = { tag = ALG }
			NOT = { tag = ALM }
			OR = {
				NOT = { exists = MOR }
				AND = {
					overlord_of = MOR
					MOR = { is_subject_other_than_tributary_trigger = yes }
				}
			}
		}
		provinces_to_highlight = {
			OR = {
				area = fez_area
				area = marrekesh_area
				area = sus_area
				area = tafilalt_area
				area = anfa_area
			}
		}
		allow = {
			is_at_war = no
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			OR = {
				check_variable = { which = "Demesne_in_Morocco" value = 15 }
				check_variable = { which = "Cores_on_Morocco" value = 12 }
			}
			OR = {
				adm_tech = 30
				AND = {
					ADM = 5
					OR = {
						advisor = statesman_CL
						advisor = statesman_BG
						advisor = statesman_NO
						advisor = statesman_BU
						advisor = statesman_TR
					}
				}
			}
			owns = 4075 #Marrakesh
		}
		effect = {
			if = {
				limit = {
					overlord_of = MOR
					MOR = { is_subject_other_than_tributary_trigger = yes }
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:inherited [MOR.GetName] while forming [MOR.GetName] via decision:form_morocco"
				inherit = MOR
			}
			fez_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MOR } add_core = MOR }
			fez_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MOR }
			marrekesh_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MOR } add_core = MOR }
			marrekesh_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MOR }
			sus_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MOR } add_core = MOR }
			sus_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MOR }
			tafilalt_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MOR } add_core = MOR }
			tafilalt_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MOR }
			anfa_area = { limit = { owned_by = ROOT } hidden_effect = { remove_core = MOR } add_core = MOR }
			anfa_area = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = MOR }

			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [MOR.GetName] via decision:form_morocco"
			ChangeTagEffect = { scope=MOR }
			Effect_set_capital = { target=4075 }	#Marrakesh
			if = {
				limit = {
					technology_group = soudantech
				}
				change_technology_group = muslim
				change_unit_type = muslim
			}
			if = {
				limit = {
					government = monarchy
					NOT = {
						has_country_modifier = title_5
						has_country_modifier = title_6
					}
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 5 (Kingdom equivalent):form_morocco"
				change_title_5 = yes
			}
			add_absolutism = 10
			swap_national_ideas_effect = yes
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	form_julich_berg_kleve = { #Already exists in missions, to be removed when the relevant missions get added back in
		major = yes
		potential = {
			OR = {
				tag = JUL
				tag = BRG
				tag = KLE
			}
		}
		provinces_to_highlight = {
			OR = {
				province_id = 51
				province_id = 1253
				province_id = 2623
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			is_at_war = no
			owns = 51 # Kleve
			owns = 1253 # Julich
			owns = 2623 # Berg
		}
		effect = {
			51 = { add_core = JBC } # Kleve
			1253 = { add_core = JBC } # Julich
			2623 = { add_core = JBC } # Berg
			2637 = { add_permanent_claim = JBC } # Mark
			add_prestige = 20
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [JBC.GetName] via decision:form_julich_berg_kleve"
			ChangeTagEffect = { scope=JBC }
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = JBC_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	become_brandenburg = {
		major = yes
		potential = {
			OR = {
				tag = HOH
				tag = ANS
				tag = BYR
			}
		}
		provinces_to_highlight = {
			province_id = 50
		}
		allow = {
			is_at_war = no
			NOT = { exists = BRA }
			owns = 50 # Brandenburg
		}
		effect = {
			50 = { add_core = BRA } # Brandenburg
			66 = { add_core = BRA } # Altmark
			264 = { add_core = BRA } # Neumark
			2621 = { add_core = BRA } # Prignitz
			2835 = { add_core = BRA } # Lubusz
			4015 = { add_core = BRA } # Havelland
			Effect_set_capital = { target=50 }
			add_prestige = 20
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [BRA.GetName] via decision:become_brandenburg"
			ChangeTagEffect = { scope=BRA }
			change_primary_culture = capital
			if = {
				limit = {
					OR = {
						has_country_modifier = title_1
						has_country_modifier = title_2
					}
				}
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 3:become_brandenburg"
				change_title_3 = yes
			}
			add_absolutism = 10
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = elbe_river_german_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
		
	form_ava = {
		major = yes
		potential = {
			OR = {
				tag = SAG
				tag = PIN
			}
			NOT = { exists = AVA }
		}
		provinces_to_highlight = {
			OR = {
				province_id = 584
				province_id = 2429
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			is_at_war = no
			owns_core_province = 584 # Ava
			owns_core_province = 2429 # Sagaing
			OR = {
				AND = {
					tag = SAG
					NOT = { exists = PIN }
				}
				AND = {
					tag = PIN
					NOT = { exists = SAG }
				}
			}
		}
		effect = {
			every_province = {
				limit = {
					OR = {
						region = inland_burma_region
						region = coastal_burma_region
					}
					is_core = ROOT
				}
				add_core = AVA
			}
			add_prestige = 20
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [AVA.GetName] via decision:form_ava"
			ChangeTagEffect = { scope=AVA }
			add_absolutism = 10
			Effect_set_capital = { target=584 }
			584 = { rename_capital = "Awa" }
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
	
	adopt_mamluk_government = {
		major = yes
		potential = {
			religion_group = muslim
			capital_scope = { continent = mena }
			NOT = { exists = EGY }
			egypt_region = {
				owned_by = PREV
			}
			NOT = { has_country_flag = formed_mamluks }
			#NOT = { tag = HLR }
			NOT = { tag = PAP }
			NOT = { tag = TUR }
			NOT = { tag = BYZ }
			NOT = { tag = ROM }
			NOT = { tag = OTT }
			NOT = { tag = ARB }
			NOT = { tag = MAM }
			NOT = { has_country_flag = adopted_mamluk_govt }
			#NOT = { has_institution = enlightenment }
		}
		provinces_to_highlight = {
			province_id = 361
		}
		allow = {
			NOT = { exists = MAM } #To Show why it might disappear
			owns_core_province = 361
		}
		effect = {
			Effect_set_capital = { target=361 } # Cairo
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [MAM.GetName] via decision:adopt_mamluk_government"
			ChangeTagEffect = { scope=MAM }
			if = {
				limit = { has_regency = no }
				kill_ruler = yes
			}
			if = {
				limit = { has_heir = yes }
				kill_heir = {}
			}
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = MAM_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
			#electivisation_effect = yes
			egypt_region = {
				limit = {
					NOT = { is_core = MAM }
					NOT = { is_claim = MAM }
				}
				add_permanent_claim = MAM
			}
			set_country_flag = formed_mamluks
		}
		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				adm_tech = 30
			}
		}
	}
	
	form_ilkhanate = {
		major = yes
		potential = {
			NOT = { exists = ILK }
			OR = {
				culture_group = tartar_group
				culture_group = altaic
				primary_culture = turkmeni
				tag = ISF
				tag = CHU
				tag = JAI
				tag = JQB
			}
			capital_scope = {
				OR = {
					region = lower_mesopotamia_region
					region = upper_mesopotamia_region
					region = south_caucasus_region
					superregion = persia_superregion
				}
			}
			OR = {
				ai = no
				NOT = { tag = TIM }
			}
		}
		provinces_to_highlight = {
			OR = {
				region = south_anatolia_region
				region = north_anatolia_region
				region = south_caucasus_region
				region = lower_mesopotamia_region
				region = upper_mesopotamia_region
				superregion = persia_superregion
			}
		}
		allow = {
			custom_trigger_tooltip = {tooltip ="is_free_or_tributary_trigger_tooltip" is_free_or_tributary_trigger = yes }
			OR = {
				check_variable = { which = "Demesne_in_Ilkhanate_Region" value = 70 }
				check_variable = { which = "Cores_on_Ilkhanate_Region" value = 50 }
			}
			owns = 410 # Baghdad
			owns = 416 # Tabriz
			owns = 1315 # Soltaniyeh
			owns = 429 # Shiraz
			owns = 414 # Isfahan
			owns = 1313 # Tehran
			owns = 4234 # Nishapur
			is_at_war = no
		}
		effect = {
			lower_mesopotamia_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = ILK } add_core = ILK }
			lower_mesopotamia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ILK }
			iran_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = ILK } add_core = ILK }
			iran_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ILK }
			fars_region = { limit = { owned_by = ROOT } hidden_effect = { remove_core = ILK } add_core = ILK }
			fars_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ILK }
			upper_mesopotamia_region = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ILK }
			persia_superregion = { limit = { NOT = { owned_by = ROOT } } add_permanent_claim = ILK }
			south_anatolia_region = { limit = { NOT = { is_core = ROOT } } add_permanent_claim = ILK }
			north_anatolia_region = { limit = { NOT = { is_core = ROOT } } add_permanent_claim = ILK }
			south_caucasus_region = { limit = { NOT = { is_core = ROOT } } add_permanent_claim = ILK }
			add_prestige = 15
			if = {
				limit = { government = monarchy NOT = { has_country_modifier = title_6 } }
				log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:increased title level to 6 (Empire equivalent):form_ilkhanate"
				change_title_6 = yes
			}
			add_absolutism = 10
			log = "FE-FormingNewNations:[GetYear]:[Root.GetName]:formed [ILK.GetName] via decision:form_ilkhanate"			
			ChangeTagEffect = { scope=ILK }
			Effect_set_capital = { target=1315 }
			#persian_group_union_effect = yes
			#armenian_union_effect = yes
			
			#if = {
			#	limit = {
			#		has_custom_ideas = no
			#		NOT = { has_idea_group = ILK_ideas }
			#	}
			#	swap_national_ideas_effect = yes
			#}
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	

}
