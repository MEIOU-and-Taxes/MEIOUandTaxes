#by Shiro
#refactored by Corlis

Do_Invest = {
    set_key = { lhs = Tmp_Misc0 value = 0 }
    set_key = { lhs = Public_Tmp1 value = 0 }
    set_key = { lhs = Public_Tmp2 value = 0 }
    set_key = { lhs = Public_Tmp3 value = 0 }
    set_key = { lhs = Public_Tmp4 value = 0 }
    set_key = { lhs = Public_Tmp5 value = 0 }
    set_key = { lhs = Public_Tmp6 value = 0 }
    
    if = {
        limit = {
            ai = yes
        }
        export_to_key = {
            lhs = Public_Tmp1
            value = treasury
        }
        export_to_key = {
            lhs = Public_Tmp2
            value = monthly_income
        }
        
        multiply_key = { lhs = Public_Tmp2 value = 12 }        		 # `DEFINE_AI_AUTOINVESTOR_MONTHS_OF_INCOME_RETAINED~value`
        subtract_key = { lhs = Public_Tmp1 which = Public_Tmp2 }	 # treasury - X years of income
        set_key = 	   { lhs = Public_Tmp3 value = 0.35 }			 # `DEFINE_AI_AUTOINVESTOR_PERCENTAGE_USAGE_OF_BASE_BUDGET~value`
        
        Modi_AiInvestBudget = yes #this modifies base budget percentage (Public_Tmp3) based on ruler personality
        
        multiply_key = { lhs = Public_Tmp1 which = Public_Tmp3 }  # this is this year's budget	
        set_key = { lhs = Ai_Invest_BudgetFixed which = Public_Tmp1 }                     
    }
    else = {
        export_to_key = {
            lhs = Ai_Invest_BudgetFixed
            value = treasury
        }
        set_key = { lhs = Tmp_Misc0 which = Ai_Invest_Budget }
        multiply_key = { lhs = Tmp_Misc0 value = 0.01 }
        multiply_key = { lhs = Ai_Invest_BudgetFixed which = Tmp_Misc0 }
    }
  
    if = {
        limit = { #do not invest if investment budget is below 5 ducats (?)
            check_key = { lhs = Ai_Invest_BudgetFixed value = 5 }
        }
        log = "SYS-StateInvestmentBudget:[This.GetName]::investment budget of:[GV_AI_Invest_BudgetFixed]:investment going forward"

        set_key = { lhs = Public_Tmp1 value = 0 }
        set_key = { lhs = Public_Tmp2 value = 0 }
        set_key = { lhs = Public_Tmp3 value = 0 }
        set_key = { lhs = Public_Tmp4 value = 0 }
        set_key = { lhs = Public_Tmp5 value = 0 }
        set_key = { lhs = Public_Tmp6 value = 0 }
        every_owned_province = {
            limit = {
                OR = {
                    has_province_flag = Invest
                    owner = { ai = yes }
                }
                controlled_by = owner
                is_core = owner
                check_key = { lhs = Prov_BUPow value = 2.5 }    #NEVER invest under 2.5 State Reach (investment pool won't drain)
            }
            clr_province_flag = invested
            set_province_flag = possible_investment
            set_key = { lhs = Public_Tmp1 value = 0 }
            set_key = { lhs = Public_Tmp2 value = 0 }
            set_key = { lhs = Public_Tmp3 value = 0 }
            set_key = { lhs = Public_Tmp4 value = 0 }
            set_key = { lhs = Public_Tmp5 value = 0 }
            set_key = { lhs = Public_Tmp6 value = 0 }
            
            #Resources have a cap
            Infra_AISpendCheck_Rural = { type = Agriculture return = Public_Tmp1 cap = Building_AgricultureMax perc = 0.95 }
            Infra_AISpendCheck_Rural = { type = Forestry return = Public_Tmp2 cap = Building_ForestryMax perc = 0.70 }
            Infra_AISpendCheck_Rural = { type = Extraction return = Public_Tmp3 cap = Building_ExtractionMax perc = 0.95 }
            Infra_AISpendCheck_Rural = { type = Fishery return = Public_Tmp4  cap = Building_FisheryMax perc = 0.90 }
            #Urbans are potentially uncapped
            # Only player can set the NIndus flag
            if = {
                limit = {
                    NOT = {
                        has_province_flag = NIndus
                    } 
                }
                #Urbans are potentially uncapped
                Infra_AISpendCheck_Urban = { type = Industrial return = Public_Tmp5 Throughput = 65 }
                #Disabled Academic, as its basically pointless to invest
                Infra_AISpendCheck_Urban = { type = Commercial return = Public_Tmp6 Throughput = 80 } 
            }
            #scores of every province added to the country
            PREV = { 
                change_key = { lhs = Public_Tmp1 which = PREV } 
                change_key = { lhs = Public_Tmp2 which = PREV }
                change_key = { lhs = Public_Tmp3 which = PREV }
                change_key = { lhs = Public_Tmp4 which = PREV }
                change_key = { lhs = Public_Tmp5 which = PREV }
                change_key = { lhs = Public_Tmp6 which = PREV }
            }    
        }
        #logging summed score on country level
        #_#_log = "SYS-StateInvestmentScore:[GetYear]:[This.GetName]::Agriculture:Forestry:Extraction:Fishery:Industrial:Academic"
        #_#_log = "SYS-StateInvestmentScore:[GetYear]:[This.GetName]::[GV_Public_Tmp1]:[GV_Public_Tmp2]:[GV_Public_Tmp3]:[GV_Public_Tmp4]:[GV_Public_Tmp5]:[GV_Public_Tmp6]"
        #sum all scores into 1
        set_key = { lhs = Public_Tmp0 which = Public_Tmp1 }
        change_key = { lhs = Public_Tmp0 which = Public_Tmp2 }
        change_key = { lhs = Public_Tmp0 which = Public_Tmp3 }
        change_key = { lhs = Public_Tmp0 which = Public_Tmp4 }
        change_key = { lhs = Public_Tmp0 which = Public_Tmp5 }
        change_key = { lhs = Public_Tmp0 which = Public_Tmp6 }
        
        set_key = { lhs = Public_Tmp9 which = Public_Tmp0 } #save total score here
        set_key = { lhs = Public_Tmp0 which = Ai_Invest_BudgetFixed } 
        divide_key = { lhs = Public_Tmp0 which = Public_Tmp9 } #budget divided by total score = ducats per score

        if = {
            limit = {
                is_key_equal = { lhs = Public_Tmp0 value = 0 } # The ducats per score value is lower than 0.001 (e.g. 2000 total score, 1 ducat invest => bug)
            }
            # If we run into this, we add another 1k multiplier, so its 1m total score limit per 1 ducat.
            set_country_flag = ducats_per_score_exceeds_limit
            set_key = { lhs = Public_Tmp0 which = Ai_Invest_BudgetFixed } 
            multiply_key = { lhs = Public_Tmp0 value = 1000 }
            divide_key = { lhs = Public_Tmp0 which = Public_Tmp9 }
            
            # If we run into this again, we turn it off completely and swap back to the slower divide calc
            if = {
                limit = {
                    is_key_equal = { lhs = Public_Tmp0 value = 0 } # The ducats per score value is lower than 0.001 (e.g. 2000000 total score, 1 ducat invest => bug)
                }
                 set_key = { lhs = Public_Tmp0 which = Public_Tmp9 } 
                 divide_key = { lhs = Public_Tmp0 which = Ai_Invest_BudgetFixed }
                 clr_country_flag = ducats_per_score_exceeds_limit
                 set_country_flag = score_per_ducat_calc
            }
        }
        
        
        #log = "SYS-StateInvestment:[GetYear]:[This.GetName]:budget divided by total score = [GV_Ai_Invest_BudgetFixed]/[GV_Public_Tmp9] = [GV_Public_Tmp0]"

        set_key = { lhs = Tmp_9 value = 0 }
        set_key = { lhs = Public_Tmp7 value = 0 } #Key to be used to substract treasury
        #_#_log = "SYS-StateInvestmentDucat:Year:Country:Province:Agriculture:Forestry:Extraction:Fishery:Industrial:Academic:IndustrialUp:IndustrialDown:IndustrialGross"

        # Execute investment only in provinces where you actually have any scoring
        every_owned_province = {
            limit = {
                has_province_flag = possible_investment
                PREV = { NOT = { is_key_equal = { lhs = Public_Tmp9 value = 0 } } }
            }
            set_key = { lhs = Tmp_9 value = 0 }
                # set total score per province of each type of building (the one that came from Infra_AISpendCheck)
            set_key = { lhs = Tmp_1 which = Public_Tmp1 } 
            set_key = { lhs = Tmp_2 which = Public_Tmp2 }
            set_key = { lhs = Tmp_3 which = Public_Tmp3 }
            set_key = { lhs = Tmp_4 which = Public_Tmp4 }
            set_key = { lhs = Tmp_5 which = Public_Tmp5 }
            set_key = { lhs = Tmp_6 which = Public_Tmp6 }
            
            # calculate each score's percentage % from the total country's score for each type of building
            set_key = { lhs = Public_Tmp9 which = PREV } #total country score
            divide_key = { lhs = Tmp_1 which = Public_Tmp9 } 
            divide_key = { lhs = Tmp_2 which = Public_Tmp9 }
            divide_key = { lhs = Tmp_3 which = Public_Tmp9 }
            divide_key = { lhs = Tmp_4 which = Public_Tmp9 }
            divide_key = { lhs = Tmp_5 which = Public_Tmp9 }
            divide_key = { lhs = Tmp_6 which = Public_Tmp9 }
            
            # tmp_1 is how much % of the total score of the entire country represents this province, #tmp_2 fishery, etc. in raw percentage value
            #multiply_key = { lhs = Tmp_1 value = 100 }
            #multiply_key = { lhs = Tmp_2 value = 100 }
            #multiply_key = { lhs = Tmp_3 value = 100 }
            #multiply_key = { lhs = Tmp_4 value = 100 }
            #multiply_key = { lhs = Tmp_5 value = 100 }
            #multiply_key = { lhs = Tmp_6 value = 100 }
            
            # if more than 60% of total score goes into 1 specific industry, set a flag to next time reduce score
            if = {
                limit = {
                    is_capital = no
                }
                if = {
                    limit = {
                        check_key = { lhs = Tmp_1 value = 0.60 }
                    }
                    if = {
                        limit = {
                            check_key = { lhs = Tmp_1 value = 0.80 }
                        }
                        set_province_flag = AgricultureInvestment8
                    }
                    else = {
                        set_province_flag = AgricultureInvestment6
                    }
                }
                else_if = {
                    limit = {
                        check_key = { lhs = Tmp_2 value = 0.60 }
                    }
                    if = {
                        limit = {
                            check_key = { lhs = Tmp_2 value = 0.80 }
                        }
                        set_province_flag = ForestryInvestment8
                    }
                    else = {
                        set_province_flag = ForestryInvestment6
                    }
                }
                else_if = {
                    limit = {
                        check_key = { lhs = Tmp_3 value = 0.60 }
                    }
                    if = {
                        limit = {
                            check_key = { lhs = Tmp_3 value = 0.80 }
                        }
                        set_province_flag = ExtractionInvestment8
                    }
                    else = {
                        set_province_flag = ExtractionInvestment6
                    }
                }
                else_if = {
                    limit = {
                        check_key = { lhs = Tmp_4 value = 0.60 }
                    }
                    if = {
                        limit = {
                            check_key = { lhs = Tmp_4 value = 0.80 }
                        }
                        set_province_flag = FisheryInvestment8
                    }
                    else = {
                        set_province_flag = FisheryInvestment6
                    }
                }
                else_if = {
                    limit = {
                        check_key = { lhs = Tmp_5 value = 0.60 }
                    }
                    if = {
                        limit = {
                            check_key = { lhs = Tmp_5 value = 0.80 }
                        }
                        set_province_flag = IndustrialInvestment8
                    }
                    else = {
                        set_province_flag = IndustrialInvestment6
                    }
                }
                else_if = {
                    limit = {
                        check_key = { lhs = Tmp_6 value = 0.60 }
                    }
                    if = {
                        limit = {
                            check_key = { lhs = Tmp_6 value = 0.80 }
                        }
                        set_province_flag = CommercialInvestment8
                    }
                    else = {
                        set_province_flag = CommercialInvestment6
                    }
                }
            }

            # calculate score / total budget to get in raw money value how much it will be invested
            set_key = { lhs = Public_Tmp0 which = PREV } # total budget
            
            if = {
                limit = {
                    owner = { has_country_flag = score_per_ducat_calc }
                }
                if = {
                    limit = {
                        check_key = { lhs = Public_Tmp0 value = 0.001 }
                    }
                    divide_key = { lhs = Public_Tmp1 which = Public_Tmp0 }
                    divide_key = { lhs = Public_Tmp2 which = Public_Tmp0 }
                    divide_key = { lhs = Public_Tmp3 which = Public_Tmp0 }
                    divide_key = { lhs = Public_Tmp4 which = Public_Tmp0 }
                    divide_key = { lhs = Public_Tmp5 which = Public_Tmp0 }
                    divide_key = { lhs = Public_Tmp6 which = Public_Tmp0 }   
                }
            } else_if = {
                limit = {
                    check_key = { lhs = Public_Tmp0 value = 0.001 }
                }
                multiply_key = { lhs = Public_Tmp1 which = Public_Tmp0 }
                multiply_key = { lhs = Public_Tmp2 which = Public_Tmp0 }
                multiply_key = { lhs = Public_Tmp3 which = Public_Tmp0 }
                multiply_key = { lhs = Public_Tmp4 which = Public_Tmp0 }
                multiply_key = { lhs = Public_Tmp5 which = Public_Tmp0 }
                multiply_key = { lhs = Public_Tmp6 which = Public_Tmp0 }   

                if = {
                    limit = {
                        owner = { has_country_flag = ducats_per_score_exceeds_limit }
                    }
                    multiply_key = { lhs = Public_Tmp1 value = 0.001 }
                    multiply_key = { lhs = Public_Tmp2 value = 0.001 }
                    multiply_key = { lhs = Public_Tmp3 value = 0.001 }
                    multiply_key = { lhs = Public_Tmp4 value = 0.001 }
                    multiply_key = { lhs = Public_Tmp5 value = 0.001 }
                    multiply_key = { lhs = Public_Tmp6 value = 0.001 }   
                }
            }

            #invest
            #log = "SYS-StateInvestmentDucat:[GetYear]:[This.Owner.GetName]:[This.GetName]:[GV_Public_Tmp1]:[GV_Public_Tmp2]:[GV_Public_Tmp3]:[GV_Public_Tmp4]:[GV_Public_Tmp5]:[GV_Public_Tmp6]:[GV_Building_IndustrialUp]:[GV_Building_IndustrialDown]:[GV_Building_IndustrialGross]"
            log = "SYS-StateInvestmentDucat:[This.Owner.GetName]:[This.GetName]: Agriculture: [GV_Public_Tmp1] Forestry: [GV_Public_Tmp2] Extraction: [GV_Public_Tmp3] Fishery: [GV_Public_Tmp4] Industrial: [GV_Public_Tmp5] Academic: [GV_Public_Tmp6]"
            Do_Invest_PlayerAI = { type=Agriculture investedDucatVar=Public_Tmp1 score=Tmp_1 }
            Do_Invest_PlayerAI = { type=Forestry    investedDucatVar=Public_Tmp2 score=Tmp_2 }
            Do_Invest_PlayerAI = { type=Extraction  investedDucatVar=Public_Tmp3 score=Tmp_3 }
            Do_Invest_PlayerAI = { type=Fishery     investedDucatVar=Public_Tmp4 score=Tmp_4 }
            Do_Invest_PlayerAI = { type=Industrial  investedDucatVar=Public_Tmp5 score=Tmp_5 }
            Do_Invest_PlayerAI = { type=Commercial  investedDucatVar=Public_Tmp6 score=Tmp_6 }
            clr_province_flag = possible_investment
        }

        clr_country_flag = ducats_per_score_exceeds_limit
        clr_country_flag = score_per_ducat_calc

        if = {
            limit = {
                has_country_flag = show_investment
                check_key = { lhs = Tmp_Misc0 value = 0.001 }
            }
            random_owned_province = {
                limit = {
                    has_province_flag = invested
                }
                province_event = { 
                    id = AI_Invest.9
                }
            }
        }
        POP_RemoveDucat = { type = which inp = Public_Tmp7 }        
    }    
}

Do_Invest_PlayerAI = {
    if = { 
        limit = {
            check_key = { lhs = $investedDucatVar$ value = 0.001 }
        }
        
        set_province_flag = invested
        set_key = { lhs = Tmp_9 which = PREV } # set owner's residual budget
        set_key = { lhs = Tmp_8 which = Tmp_9 } 
        
            # use the proportionate residual amount corresponding to the original score
		#multiply_key = { lhs = $score$ value = 0.01 }
        multiply_key = { lhs = Tmp_8 which = $score$ } # Tmp_8 is how much of the residual budget the property will try to spend
        
            # substract from owner's residual budget
        subtract_key = { lhs = Tmp_9 which = Tmp_8 }
        
            # re set owner's residual budget
        PREV = {
            set_key = { lhs = Tmp_9 which = PREV }
        }
            # add residual budget to the actual investment budget
        change_key =  { lhs = $investedDucatVar$ which = Tmp_8 }
        
        
        set_key = { lhs = Public_Tmp11 which = Building_$type$ }
        multiply_key = { lhs = Public_Tmp11 value = 0.25 } # maximum possible investment depending on building size
        if = {
            limit = { # if investment is higher than the cap 
                check_key = { lhs = $investedDucatVar$ which = Public_Tmp11 }
            }
                # save investment target
            set_key = { lhs = Tmp_8 which = $investedDucatVar$ }
            
                # set real investment to maximum limit
            set_key = { lhs = $investedDucatVar$ which = Public_Tmp11 }
            
                # re calculate residual budget for this case
            subtract_key = { lhs = Tmp_8 which = $investedDucatVar$ }
            
                # add it to total budget
            change_key = { lhs = Tmp_9 which = Tmp_8 }
            
                # set it to owner
            PREV = {
                set_key = { lhs = Tmp_9 which = PREV }
            }
        }
        if = { # if investment is higher than 75
            limit = {
				NOT = { is_year = 1450 }
                check_key = { lhs = $investedDucatVar$ value = 75 }
            }
                # re calculate residual budget
            set_key = { lhs = Tmp_0 which = $investedDucatVar$ }
            subtract_key = { lhs = Tmp_0 value = 75 }
            
                # add it to new residual
            change_key = { lhs = Tmp_9 which = Tmp_0 }
            
                # set it to owner
            PREV = {
                set_key = { lhs = Tmp_9 which = PREV }
            }
            
            set_key = { lhs = $investedDucatVar$ value = 75 }
        }
		else_if = { # if investment is higher than 
            limit = {
				NOT = { is_year = 1550 }
                check_key = { lhs = $investedDucatVar$ value = 100 }
            }
                # re calculate residual budget
            set_key = { lhs = Tmp_0 which = $investedDucatVar$ }
            subtract_key = { lhs = Tmp_0 value = 100 }
            
                # add it to new residual
            change_key = { lhs = Tmp_9 which = Tmp_0 }
            
                # set it to owner
            PREV = {
                set_key = { lhs = Tmp_9 which = PREV }
            }
            
            set_key = { lhs = $investedDucatVar$ value = 100 }
        }
		else_if = { # if investment is higher than 150
            limit = {
				NOT = { is_year = 1650 }
                check_key = { lhs = $investedDucatVar$ value = 150 }
            }
                # re calculate residual budget
            set_key = { lhs = Tmp_0 which = $investedDucatVar$ }
            subtract_key = { lhs = Tmp_0 value = 150 }
            
                # add it to new residual
            change_key = { lhs = Tmp_9 which = Tmp_0 }
            
                # set it to owner
            PREV = {
                set_key = { lhs = Tmp_9 which = PREV }
            }
            
            set_key = { lhs = $investedDucatVar$ value = 150 }
        }
		else_if = { # if investment is higher than 200
            limit = {
				check_key = { lhs = $investedDucatVar$ value = 200 }
            }
                # re calculate residual budget
            set_key = { lhs = Tmp_0 which = $investedDucatVar$ }
            subtract_key = { lhs = Tmp_0 value = 200 }
            
                # add it to new residual
            change_key = { lhs = Tmp_9 which = Tmp_0 }
            
                # set it to owner
            PREV = {
                set_key = { lhs = Tmp_9 which = PREV }
            }
            
            set_key = { lhs = $investedDucatVar$ value = 200 }
        }
		
        
        # Actually investing the money starts here 

        if = {
            limit = {
                owner = {
                    ai = no
                }
            }
            # Set some info for the Autoinvestor event
            set_key = { lhs = Building_$type$Invest which = $investedDucatVar$ } 
            AAA = {
                set_key = { lhs = $type$_InvestYear which = Current_Year }
                PREV = { set_key = { lhs = $type$_InvestYear which = PREV } }
            }            
        }

        # Set country scope variable with the full amount (this is deducted from country treasury)
        PREV = {
            set_key = { lhs = $investedDucatVar$ which = PREV }
            change_key = { lhs = Public_Tmp7 which = $investedDucatVar$ }
        }

        # Op hax for Midas Touched AIs, they literally make money out of nothing
        if = {
            limit = {
                owner = {
                    ruler_has_personality = midas_touched_personality
                    ai = yes
                }
            }
            multiply_key = { lhs = $investedDucatVar$ value = 1.25 }
        }        

        # Money goes into investment pool in Full
        change_key = { lhs = Building_$type$Wealth which = $investedDucatVar$ }
    }
}

Infra_AISpendCheck_Rural = {
	set_key = { lhs = Tmp_0 which = $cap$ }
	multiply_key = { lhs = Tmp_0 value = $perc$ }
    if = {
        limit = {
           NOT = { check_key = { lhs = Building_$type$ which = Tmp_0 } }
        }
        Infra_DoSpendAI_$type$ = {  type = $type$ return = $return$ }
    }
	set_key = { lhs = Tmp_0 value = 0 }
}

Infra_AISpendCheck_Urban = {
    if = {
        limit = {
            check_key = { lhs = ProdU_Throughput value = $Throughput$ }
        }
        Infra_DoSpendAI_$type$ = {  type = $type$ return = $return$ }
    }
}

Infra_DoSpendAI_Agriculture = {
	set_key = { lhs = Tmp_0 which = Building_$type$Up }
	set_key = { lhs = Tmp_7 which = Building_$type$Down }
	
	subtract_key = { lhs = Tmp_0 which = Tmp_7 } # how much the industry wants to expand vs shrink
    if = {
        limit = { #if wants to expand & it's profitable
            check_key = { lhs = Building_$type$Gross value = 0.001 }
            check_key = { lhs = Tmp_0 value = 0.01 }
        }
        set_key = { lhs = $return$ which = Tmp_0 } # Local Ai Willingness to Invest
        
        multiply_key = { lhs = $return$ which = pv_ce } # CE
        multiply_key = { lhs = $return$ value = 0.1 }
       
        if = {
            limit = {
                is_capital = yes
            }
            multiply_key = { lhs = $return$ value = 3.5 }
        }
        else_if = {
            limit = {
                any_province_in_state = {
                    is_capital = yes
                }
            }
            multiply_key = { lhs = $return$ value = 3 }
        }
        else_if = {
            limit = {
                NOT = { is_in_capital_area = yes }
            }
            multiply_key = { lhs = $return$ value = 0.5 }
        }
        
        #set_key = { lhs = Tax_Tmp0 which = Tax_CostPropAgricultureRate }
        #multiply_key = { lhs = Tax_Tmp0 value = 0.01 }
        #change_key = { lhs = Tax_Tmp0 value = 1 }
        #multiply_key = { lhs = $return$ which = Tax_Tmp0 }
        #set_key = { lhs = Tax_Tmp0 value = 0 }
        
        trigger_switch = {
            on_trigger = has_province_flag 
            AgricultureInvestment6 = { multiply_key = { lhs = $return$ value = 0.006 } clr_province_flag = AgricultureInvestment6 }
            AgricultureInvestment8 = { multiply_key = { lhs = $return$ value = 0.004 } clr_province_flag = AgricultureInvestment8 }
        }     
        
        if = { # reduce score by size -> prevents financial collapses
            limit = {
                check_key = { lhs = Building_$type$ value = 800 }
            }
            if = {
                limit = {
                    check_key = { lhs = Building_$type$ value = 1200 }
                }
                if = {
                    limit = {
                        check_key = { lhs = Building_$type$ value = 1600 }
                    }
                    multiply_key = { lhs = $return$ value = 0.05 }
                }
                else = {
                    multiply_key = { lhs = $return$ value = 0.15 }
                }
            }
            else = {
                multiply_key = { lhs = $return$ value = 0.25 }
            }
        }
        else_if = {
            limit = {
                check_key = { lhs = Building_$type$ value = 350 }
            }
            if = {
                limit = {
                    check_key = { lhs = Building_$type$ value = 500 }
                }
                if = {
                    limit = {
                        check_key = { lhs = Building_$type$ value = 650 }
                    }
                    multiply_key = { lhs = $return$ value = 0.3 }
                }
                else = {
                    multiply_key = { lhs = $return$ value = 0.4 }
                }
            }
            else = {
                multiply_key = { lhs = $return$ value = 0.5 }
            }
        }
    }
    else = {
        set_key = { lhs = $return$ value = 0 }
    }
}

Infra_DoSpendAI_Forestry = {
	set_key = { lhs = Tmp_0 which = Building_$type$Up }
	set_key = { lhs = Tmp_7 which = Building_$type$Down }
	
	subtract_key = { lhs = Tmp_0 which = Tmp_7 } # how much the industry wants to expand vs shrink
    if = {
        limit = { #if wants to expand & it's profitable
            check_key = { lhs = Building_$type$Gross value = 0.001 }
            check_key = { lhs = Tmp_0 value = 0.01 }
        }
        set_key = { lhs = $return$ which = Tmp_0 } # Local Ai Willingness to Invest
        
        multiply_key = { lhs = $return$ which = pv_ce } # CE
        multiply_key = { lhs = $return$ value = 0.1 }
        
        if = {
            limit = {
                is_capital = yes
            }
            multiply_key = { lhs = $return$ value = 3.5 }
        }
        else_if = {
            limit = {
                any_province_in_state = {
                    is_capital = yes
                }
            }
            multiply_key = { lhs = $return$ value = 3 }
        }
        else_if = {
            limit = {
                NOT = { is_in_capital_area = yes }
            }
            multiply_key = { lhs = $return$ value = 0.5 }
        }
        
        #set_key = { lhs = Tax_Tmp0 which = Tax_CostPropForestryRate }
        #multiply_key = { lhs = Tax_Tmp0 value = 0.01 }
        #change_key = { lhs = Tax_Tmp0 value = 1 }
        #multiply_key = { lhs = $return$ which = Tax_Tmp0 }
        #set_key = { lhs = Tax_Tmp0 value = 0 }
        
        trigger_switch = {
            on_trigger = has_province_flag 
            ForestryInvestment6 = { multiply_key = { lhs = $return$ value = 0.006 } clr_province_flag = ForestryInvestment6 }
            ForestryInvestment8 = { multiply_key = { lhs = $return$ value = 0.004 } clr_province_flag = ForestryInvestment8 }
        }
           
        if = { # reduce score by size -> prevents financial collapses
            limit = {
                check_key = { lhs = Building_$type$ value = 800 }
            }
            if = {
                limit = {
                    check_key = { lhs = Building_$type$ value = 1200 }
                }
                if = {
                    limit = {
                        check_key = { lhs = Building_$type$ value = 1600 }
                    }
                    multiply_key = { lhs = $return$ value = 0.05 }
                }
                else = {
                    multiply_key = { lhs = $return$ value = 0.15 }
                }
            }
            else = {
                multiply_key = { lhs = $return$ value = 0.25 }
            }
        }
        else_if = {
            limit = {
                check_key = { lhs = Building_$type$ value = 350 }
            }
            if = {
                limit = {
                    check_key = { lhs = Building_$type$ value = 500 }
                }
                if = {
                    limit = {
                        check_key = { lhs = Building_$type$ value = 650 }
                    }
                    multiply_key = { lhs = $return$ value = 0.3 }
                }
                else = {
                    multiply_key = { lhs = $return$ value = 0.4 }
                }
            }
            else = {
                multiply_key = { lhs = $return$ value = 0.5 }
            }
        }
    }
    else = {
        set_key = { lhs = $return$ value = 0 }
    }
}

Infra_DoSpendAI_Extraction = {
	set_key = { lhs = Tmp_0 which = Building_$type$Up }
	set_key = { lhs = Tmp_7 which = Building_$type$Down }
	
	subtract_key = { lhs = Tmp_0 which = Tmp_7 } # how much the industry wants to expand vs shrink
    if = {
        limit = { #if wants to expand & it's profitable
            check_key = { lhs = Building_$type$Gross value = 0.001 }
            check_key = { lhs = Tmp_0 value = 0.01 }
        }
        set_key = { lhs = $return$ which = Tmp_0 } # Local Ai Willingness to Invest
        
        multiply_key = { lhs = $return$ which = pv_ce } # CE
        multiply_key = { lhs = $return$ value = 0.1 }
        
        if = {
            limit = {
                is_capital = yes
            }
            multiply_key = { lhs = $return$ value = 3.5 }
        }
        else_if = {
            limit = {
                any_province_in_state = {
                    is_capital = yes
                }
            }
            multiply_key = { lhs = $return$ value = 3 }
        }
        else_if = {
            limit = {
                NOT = { is_in_capital_area = yes }
            }
            multiply_key = { lhs = $return$ value = 0.5 }
        }
		
		if = {
			limit = {
				OR = {
					has_province_flag = pm
					has_province_flag = supersalt
				}
			}
			multiply_key = { lhs = $return$ value = 2 }
		}
        
        #set_key = { lhs = Tax_Tmp0 which = Tax_CostPropExtractionRate }
        #multiply_key = { lhs = Tax_Tmp0 value = 0.01 }
        #change_key = { lhs = Tax_Tmp0 value = 1 }
        #multiply_key = { lhs = $return$ which = Tax_Tmp0 }
        #set_key = { lhs = Tax_Tmp0 value = 0 }
        
        trigger_switch = {
            on_trigger = has_province_flag 
            ExtractionInvestment6 = { multiply_key = { lhs = $return$ value = 0.006 } clr_province_flag = ExtractionInvestment6 }
            ExtractionInvestment8 = { multiply_key = { lhs = $return$ value = 0.004 } clr_province_flag = ExtractionInvestment8 }
        }
        
        
        if = { # reduce score by size -> prevents financial collapses
            limit = {
                check_key = { lhs = Building_$type$ value = 800 }
            }
            if = {
                limit = {
                    check_key = { lhs = Building_$type$ value = 1200 }
                }
                if = {
                    limit = {
                        check_key = { lhs = Building_$type$ value = 1600 }
                    }
                    multiply_key = { lhs = $return$ value = 0.05 }
                }
                else = {
                    multiply_key = { lhs = $return$ value = 0.15 }
                }
            }
            else = {
                multiply_key = { lhs = $return$ value = 0.25 }
            }
        }
        else_if = {
            limit = {
                check_key = { lhs = Building_$type$ value = 350 }
            }
            if = {
                limit = {
                    check_key = { lhs = Building_$type$ value = 500 }
                }
                if = {
                    limit = {
                        check_key = { lhs = Building_$type$ value = 650 }
                    }
                    multiply_key = { lhs = $return$ value = 0.3 }
                }
                else = {
                    multiply_key = { lhs = $return$ value = 0.4 }
                }
            }
            else = {
                multiply_key = { lhs = $return$ value = 0.5 }
            }
        }
    }
    else = {
        set_key = { lhs = $return$ value = 0 }
    }
}

Infra_DoSpendAI_Fishery = {
	set_key = { lhs = Tmp_0 which = Building_$type$Up }
	set_key = { lhs = Tmp_7 which = Building_$type$Down }
	
	subtract_key = { lhs = Tmp_0 which = Tmp_7 } # how much the industry wants to expand vs shrink
    if = {
        limit = { #if wants to expand & it's profitable
            check_key = { lhs = Building_$type$Gross value = 0.001 }
            check_key = { lhs = Tmp_0 value = 0.01 }
        }
        set_key = { lhs = $return$ which = Tmp_0 } # Local Ai Willingness to Invest
        
        multiply_key = { lhs = $return$ which = pv_ce } # CE
        multiply_key = { lhs = $return$ value = 0.1 }		
        
        if = {
            limit = {
                is_capital = yes
            }
            multiply_key = { lhs = $return$ value = 3.5 }
        }
        else_if = {
            limit = {
                any_province_in_state = {
                    is_capital = yes
                }
            }
            multiply_key = { lhs = $return$ value = 3 }
        }
        else_if = {
            limit = {
                NOT = { is_in_capital_area = yes }
            }
            multiply_key = { lhs = $return$ value = 0.5 }
        }
		if = {
			limit = {
				OR = {
					has_province_flag = pearls_large
					has_province_flag = pearls_medium
					has_province_flag = pearls
				}
			}
			multiply_key = { lhs = $return$ value = 2 }
		}

        #set_key = { lhs = Tax_Tmp0 which = Tax_CostPropFisheryRate }
        #multiply_key = { lhs = Tax_Tmp0 value = 0.01 }
        #change_key = { lhs = Tax_Tmp0 value = 1 }
        #multiply_key = { lhs = $return$ which = Tax_Tmp0 }
        #set_key = { lhs = Tax_Tmp0 value = 0 }
        
        trigger_switch = {
            on_trigger = has_province_flag 
            FisheryInvestment6 = { multiply_key = { lhs = $return$ value = 0.006 } clr_province_flag = FisheryInvestment6 }
            FisheryInvestment8 = { multiply_key = { lhs = $return$ value = 0.004 } clr_province_flag = FisheryInvestment8 }
        }
           
        if = { # reduce score by size -> prevents financial collapses
            limit = {
                check_key = { lhs = Building_$type$ value = 800 }
            }
            if = {
                limit = {
                    check_key = { lhs = Building_$type$ value = 1200 }
                }
                if = {
                    limit = {
                        check_key = { lhs = Building_$type$ value = 1600 }
                    }
                    multiply_key = { lhs = $return$ value = 0.05 }
                }
                else = {
                    multiply_key = { lhs = $return$ value = 0.15 }
                }
            }
            else = {
                multiply_key = { lhs = $return$ value = 0.25 }
            }
        }
        else_if = {
            limit = {
                check_key = { lhs = Building_$type$ value = 350 }
            }
            if = {
                limit = {
                    check_key = { lhs = Building_$type$ value = 500 }
                }
                if = {
                    limit = {
                        check_key = { lhs = Building_$type$ value = 650 }
                    }
                    multiply_key = { lhs = $return$ value = 0.3 }
                }
                else = {
                    multiply_key = { lhs = $return$ value = 0.4 }
                }
            }
            else = {
                multiply_key = { lhs = $return$ value = 0.5 }
            }
        }
    }
    else = {
        set_key = { lhs = $return$ value = 0 }
    }
}

Infra_DoSpendAI_Industrial = {
	set_key = { lhs = Tmp_0 which = Building_$type$Up }
	set_key = { lhs = Tmp_7 which = Building_$type$Down }
	
	subtract_key = { lhs = Tmp_0 which = Tmp_7 } # how much the industry wants to expand vs shrink
    if = {
        limit = { #if wants to expand & it's profitable
            check_key = { lhs = Building_$type$Gross value = 0.001 }
            check_key = { lhs = Tmp_0 value = 0.01 }
        }
        set_key = { lhs = $return$ which = Tmp_0 } # Local Ai Willingness to Invest
        
        multiply_key = { lhs = $return$ which = pv_ce } # CE
        multiply_key = { lhs = $return$ value = 0.1 }
        
        #if = {
        #    limit = { 
        #        owner = { ai = yes }
        #    }
        #    multiply_key = { lhs = $return$ value = 0.4 } # because of industrial
        #}
        
        if = {
            limit = {
                is_capital = yes
            }
            multiply_key = { lhs = $return$ value = 5 }
        }
        else_if = {
            limit = {
                is_in_capital_area = yes
            }
            multiply_key = { lhs = $return$ value = 4 }
        }
        else_if = {
            limit = {
                NOT = { is_in_capital_area = yes }
            }
            multiply_key = { lhs = $return$ value = 0.5 }
        }

        #set_key = { lhs = Tax_Tmp0 which = Tax_CostPropIndustrialRate }
        #multiply_key = { lhs = Tax_Tmp0 value = 0.01 }
        #change_key = { lhs = Tax_Tmp0 value = 1 }
        #multiply_key = { lhs = $return$ which = Tax_Tmp0 }
        #set_key = { lhs = Tax_Tmp0 value = 0 }
        
        trigger_switch = {
            on_trigger = has_province_flag 
            IndustrialInvestment6 = { multiply_key = { lhs = $return$ value = 0.005 } clr_province_flag = IndustrialInvestment6 }
            IndustrialInvestment8 = { multiply_key = { lhs = $return$ value = 0.003 } clr_province_flag = IndustrialInvestment8 }
        }
        
        
        if = { # reduce score by size -> prevents financial collapses
            limit = {
                check_key = { lhs = Building_$type$ value = 800 }
            }
            if = {
                limit = {
                    check_key = { lhs = Building_$type$ value = 1200 }
                }
                if = {
                    limit = {
                        check_key = { lhs = Building_$type$ value = 1600 }
                    }
                    multiply_key = { lhs = $return$ value = 0.05 }
                }
                else = {
                    multiply_key = { lhs = $return$ value = 0.15 }
                }
            }
            else = {
                multiply_key = { lhs = $return$ value = 0.25 }
            }
        }
        else_if = {
            limit = {
                check_key = { lhs = Building_$type$ value = 350 }
            }
            if = {
                limit = {
                    check_key = { lhs = Building_$type$ value = 500 }
                }
                if = {
                    limit = {
                        check_key = { lhs = Building_$type$ value = 650 }
                    }
                    multiply_key = { lhs = $return$ value = 0.3 }
                }
                else = {
                    multiply_key = { lhs = $return$ value = 0.4 }
                }
            }
            else = {
                multiply_key = { lhs = $return$ value = 0.5 }
            }
        }
    }
    else = {
        set_key = { lhs = $return$ value = 0 }
    }
}

Infra_DoSpendAI_Commercial = {
	set_key = { lhs = Tmp_0 which = Building_$type$Up }
	set_key = { lhs = Tmp_7 which = Building_$type$Down }
	
	subtract_key = { lhs = Tmp_0 which = Tmp_7 } # how much the industry wants to expand vs shrink
    if = {
        limit = { #if wants to expand and is profitable
            check_key = { lhs = Tmp_0 value = 0.01 }
            check_key = { lhs = Building_$type$Gross value = 0.001 }
        }
        set_key = { lhs = $return$ which = Tmp_0 } # Local Ai Willingness to Invest
        
        #Commercial invest should be agnostic to CE
        #multiply_key = { lhs = $return$ which = pv_ce } # CE
        #multiply_key = { lhs = $return$ value = 0.1 }

        multiply_key = { lhs = $return$ value = 8 } # assuming 80% CE per se
        
        if = {
            limit = {
                is_capital = yes
            }
            multiply_key = { lhs = $return$ value = 2 }
        }
        else_if = {
            limit = {
                any_province_in_state = {
                    is_capital = yes
                }
            }
            multiply_key = { lhs = $return$ value = 1.5 }
        }
        else_if = {
            limit = {
                NOT = { is_in_capital_area = yes }
            }
            multiply_key = { lhs = $return$ value = 0.5 }
        }

        #set_key = { lhs = Tax_Tmp0 which = Tax_CostPropAcademicRate }
        #multiply_key = { lhs = Tax_Tmp0 value = 0.01 }
        #change_key = { lhs = Tax_Tmp0 value = 1 }
        #multiply_key = { lhs = $return$ which = Tax_Tmp0 }
        #set_key = { lhs = Tax_Tmp0 value = 0 }
        
        trigger_switch = {
            on_trigger = has_province_flag 
            CommercialInvestment6 = { multiply_key = { lhs = $return$ value = 0.006 } clr_province_flag = CommercialInvestment6 }
            CommercialInvestment8 = { multiply_key = { lhs = $return$ value = 0.004 } clr_province_flag = CommercialInvestment8 }
        }
        
        if = { # reduce score by size -> prevents financial collapses
            limit = {
                check_key = { lhs = Building_$type$ value = 800 }
            }
            if = {
                limit = {
                    check_key = { lhs = Building_$type$ value = 1200 }
                }
                if = {
                    limit = {
                        check_key = { lhs = Building_$type$ value = 1600 }
                    }
                    multiply_key = { lhs = $return$ value = 0.05 }
                }
                else = {
                    multiply_key = { lhs = $return$ value = 0.15 }
                }
            }
            else = {
                multiply_key = { lhs = $return$ value = 0.25 }
            }
        }
        else_if = {
            limit = {
                check_key = { lhs = Building_$type$ value = 350 }
            }
            if = {
                limit = {
                    check_key = { lhs = Building_$type$ value = 500 }
                }
                if = {
                    limit = {
                        check_key = { lhs = Building_$type$ value = 650 }
                    }
                    multiply_key = { lhs = $return$ value = 0.3 }
                }
                else = {
                    multiply_key = { lhs = $return$ value = 0.4 }
                }
            }
            else = {
                multiply_key = { lhs = $return$ value = 0.5 }
            }
        }
    }
    else = {
        set_key = { lhs = $return$ value = 0 }
    }
}