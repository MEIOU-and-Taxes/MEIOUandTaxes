Concern_SetConcernHelper = {
	set_variable = { which = Tmp_0 which = $class$_Total }
	multiply_variable = { which = Tmp_0 value = $weight$ }
	
	change_variable = { which = Concern_$concern$ which = Tmp_0 }

	set_variable = { which = Tmp_0 value = 0 }
}

Concern_SetConcern = {
	set_variable = { which = Concern_$concern$ value = 0 }

	Concern_SetConcernHelper = { class = NM weight = $weightNM$ concern = $concern$ }
	Concern_SetConcernHelper = { class = SF weight = $weightSF$ concern = $concern$ }
	Concern_SetConcernHelper = { class = RE weight = $weightRE$ concern = $concern$ }
	Concern_SetConcernHelper = { class = CL weight = $weightCL$ concern = $concern$ }
	Concern_SetConcernHelper = { class = NO weight = $weightNO$ concern = $concern$ }
	Concern_SetConcernHelper = { class = BG weight = $weightBG$ concern = $concern$ }

	if = {
		limit = {
			check_variable = { which = Concern_$concern$ value = 0 }
		}
		set_variable = { which = Concern_$concern$ value = 1 }
	}
	else = {
		set_variable = { which = Concern_$concern$ value = -1 }
	}
}

Concern_DoIterMain = {
	regiongroup = {
		region = {
			limit = {
				isValidProv = yes
			}
			set_province_flag = Unrest_Update
			Concern_SetConcern = { 
				concern = Urbs
				weightNM = -1 weightSF = -1 
				weightRE = 4 
				weightCL = -1 weightNO = -1 weightBG = 4
			}
			Concern_SetConcern = { 
				concern = Diversions
				weightNM = -1 weightSF = -1 
				weightRE = -1 
				weightCL = -4 weightNO = 1 weightBG = 4
			}
			Concern_SetConcern = { 
				concern = Consultation
				weightNM = 3 weightSF = -1 
				weightRE = 0 
				weightCL = -2 weightNO = -6 weightBG = 6
			}
			
			set_variable = { which = Concern_Welfare value = 1 }

			multiply_variable = { which = Concern_WelfareTreat value = 0.95 }
			multiply_variable = { which = Concern_DiversionsTreat value = 0.95 }
			multiply_variable = { which = Concern_SpiritualityTreat value = 0.95 }
			multiply_variable = { which = Concern_ConsultationTreat value = 0.95 }
			multiply_variable = { which = Concern_HierarchyTreat value = 0.95 }
			if = {
				limit = {
					owner = { 
						OR = {
							has_country_modifier = CL_Leverage_S
							has_country_modifier = CL_Leverage_M
							has_country_modifier = CL_Leverage_L
						}
					}
				}
				change_variable = { which = Concern_SpiritualityTreat value = 1.00 }
			}
			if = {
				limit = {
					owner = { Rights_NOGov1Has = yes }
				}
				change_variable = { which = Concern_HierarchyTreat value = 0.50 }
			}
			else_if = {
				limit = {
					owner = { Rights_NOGov2Has = yes }
				}
				change_variable = { which = Concern_HierarchyTreat value = 0.25 }
			}
			else_if = {
				limit = {
					owner = { Rights_NOGov3Has = yes }
				}
				change_variable = { which = Concern_HierarchyTreat value = 0.10 }
			}
			#Concern_DoTreatSpirituality = yes
			#Concern_DoTreatConsultation = yes
			#Concern_DoTreatHierarchy = yes

			set_variable = { which = Concern_WelfareTreatR value = 0 }
			change_variable = { which = Concern_WelfareTreatR which = Concern_WelfareTreat }
			set_variable = { which = Concern_DiversionsTreatR value = 0 }
			change_variable = { which = Concern_DiversionsTreatR which = Concern_DiversionsTreat }

            if = {
                limit = {
                    NOT = {
                        has_global_flag = POP_Sim10
                    }
                }
				Concern_DoTreatWelfare = yes
				Concern_DoTreatDiversions = yes
			}

            set_variable = { which = Concern_SpiritualityTreatR value = 0 }
			change_variable = { which = Concern_SpiritualityTreatR which = Concern_SpiritualityTreat }
			set_variable = { which = Concern_ConsultationTreatR value = 0 }
			change_variable = { which = Concern_ConsultationTreatR which = Concern_ConsultationTreat }
			set_variable = { which = Concern_HierarchyTreatR value = 0 }
			change_variable = { which = Concern_HierarchyTreatR which = Concern_HierarchyTreat }
			
			
			change_variable = { which = Concern_WelfareTreatR which = Modi_TreatWelfare }
			change_variable = { which = Concern_DiversionsTreatR which = Modi_TreatDiversions }
			change_variable = { which = Concern_SpiritualityTreatR which = Modi_TreatSpirituality }
			change_variable = { which = Concern_ConsultationTreatR which = Modi_TreatConsultation }
			change_variable = { which = Concern_HierarchyTreatR which = Modi_TreatHierarchy }
			
			#subtract_variable = { which = Concern_SpiritualityTreatR value = 2 }
			#subtract_variable = { which = Concern_ConsultationTreatR value = 2 }
			subtract_variable = { which = Concern_HierarchyTreatR value = 4 }

			# Add liberalism later when that's solved
			
			multiply_variable = { which = Concern_WelfareTreatR value = 0.1 }
			multiply_variable = { which = Concern_WelfareTreatR value = 10 }
			multiply_variable = { which = Concern_DiversionsTreatR value = 0.1 }
			multiply_variable = { which = Concern_DiversionsTreatR value = 10 }
			multiply_variable = { which = Concern_SpiritualityTreatR value = 0.1 }
			multiply_variable = { which = Concern_SpiritualityTreatR value = 10 }
			multiply_variable = { which = Concern_ConsultationTreatR value = 0.1 }
			multiply_variable = { which = Concern_ConsultationTreatR value = 10 }
			multiply_variable = { which = Concern_HierarchyTreatR value = 0.1 }
			multiply_variable = { which = Concern_HierarchyTreatR value = 10 }
		}
	}
}

Concern_DoTreatWelfare = {
	Concern_DoTreatWelfareHelper = { class = NM }
	Concern_DoTreatWelfareHelper = { class = SF }
	Concern_DoTreatWelfareHelper = { class = RE }
	Concern_DoTreatWelfareHelper = { class = NO }
	Concern_DoTreatWelfareHelper = { class = BG }
	Concern_DoTreatWelfareHelper = { class = CL }

	divide_variable = { which = Tmp_0 which = Class_Total }
	multiply_variable = { which = Tmp_0 value = 15 }

	change_variable = { which = Concern_WelfareTreatR which = Tmp_0 }

	set_variable = { which = Tmp_0 value = 0 }
	set_variable = { which = Tmp_1 value = 0 }
}

Concern_DoTreatWelfareHelper = {
	set_variable = { which = Tmp_1 which = $class$_FillLifeC }
	subtract_variable = { which = Tmp_1 value = 1.1 }

	if = {
		limit = {
			check_variable = { which = $class$_FillLifeC value = 1 }
		}
		multiply_variable = { which = Tmp_1 value = 0.5 }
	}

	multiply_variable = { which = Tmp_1 which = $class$_Total }
	change_variable = { which = Tmp_0 which = Tmp_1 }
}

Concern_DoTreatDiversions = {
	if = {
		limit = {
			check_variable = { which = Class_Notables value = 0.01 }
		}
		Concern_DoTreatDiversionsHelperC = { class = NO }
		Concern_DoTreatDiversionsHelperC = { class = BG }
		Concern_DoTreatDiversionsHelperC = { class = CL }

		divide_variable = { which = Tmp_0 which = Class_Notables }

		change_variable = { which = Concern_DiversionsTreatR which = Tmp_0 }

		set_variable = { which = Tmp_0 value = 0 }
		set_variable = { which = Tmp_1 value = 0 }
		
		Concern_DoTreatDiversionsHelperL = { class = NO }
		Concern_DoTreatDiversionsHelperL = { class = BG }
		Concern_DoTreatDiversionsHelperL = { class = CL }

		divide_variable = { which = Tmp_0 which = Class_Notables }
		
		multiply_variable = { which = Tmp_0 value = 0.667 } # temporary balancing

		change_variable = { which = Concern_DiversionsTreatR which = Tmp_0 }

		set_variable = { which = Tmp_0 value = 0 }
		set_variable = { which = Tmp_1 value = 0 }
	}
}

Concern_DoTreatDiversionsHelperC = {
	set_variable = { which = Tmp_1 which = $class$_FillComfort }
	subtract_variable = { which = Tmp_1 value = 10 }

	if = {
		limit = {
			check_variable = { which = $class$_FillComfort value = 1 }
		}
		multiply_variable = { which = Tmp_1 value = 0.5 }
	}

	multiply_variable = { which = Tmp_1 which = $class$_Total }
	change_variable = { which = Tmp_0 which = Tmp_1 }
}

Concern_DoTreatDiversionsHelperL = {
	set_variable = { which = Tmp_1 which = $class$_FillLuxury }
	subtract_variable = { which = Tmp_1 value = 7.5 }

	if = {
		limit = {
			check_variable = { which = $class$_FillLuxury value = 1 }
		}
		multiply_variable = { which = Tmp_1 value = 0.5 }
	}

	multiply_variable = { which = Tmp_1 which = $class$_Total }
	change_variable = { which = Tmp_0 which = Tmp_1 }
}