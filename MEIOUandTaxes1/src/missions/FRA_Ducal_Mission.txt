
ducal_fights_centralisation = {

	slot = 4
	generic = no
	ai = yes
	
	potential = {
		is_subject_of = FRA
		NOT = {
			tag = BUR
		}
		OR = {
			is_subject_of_type = appanage_subject
			is_subject_of_type = daimyo_vassal
		}
		has_global_flag = hundred_years_war_FRA_win
		FRA = {
			NOT = { has_country_flag = had_league_public_weal }
		}
	}
	has_country_shield = yes

	league_of_public_weal = {
		icon = league_of_the_public_weal
		required_missions = { }
		position = 1
		trigger = {
			overlord = {
				all_subject_country = {
					OR = {
						tag = ROOT
						NOT = { total_development = ROOT }
					}
				}
			}
			diplomatic_reputation = 2.5
			dip = 3
		}
		effect = {
			custom_tooltip = league_of_public_weal_tt
			add_prestige = 10
		}
	}
}
